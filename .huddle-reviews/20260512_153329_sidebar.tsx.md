# src/components/sidebar.tsx (2026-05-12T15:33:34)

**種別**: Git diff (vs HEAD)


## 変更前 diff

```
diff --git a/src/components/sidebar.tsx b/src/components/sidebar.tsx
index c92e0e3..ef42e69 100644
--- a/src/components/sidebar.tsx
+++ b/src/components/sidebar.tsx
@@ -690,7 +690,6 @@ export function Sidebar({
                     <Link
                       key={ws.id}
                       href={`/${ws.slug}`}
-                      prefetch
                       onClick={() => setShowWsSwitcher(false)}
                       className={`flex items-center justify-between gap-2 px-3 py-2 text-sm truncate transition-colors rounded-lg mx-1 ${
                         ws.id === workspace.id
@@ -917,7 +916,6 @@ export function Sidebar({
           <div className="hidden lg:grid lg:grid-cols-3 gap-y-2 mx-3 mb-3 py-3 rounded-xl bg-white/[0.02] border border-border/30 [&>a]:justify-self-center [&>button]:justify-self-center">
             <Link
               href={`/${workspaceSlug}/dashboard`}
-              prefetch
               onClick={() => setSidebarOpen(false)}
               className="relative flex flex-col items-center gap-1 px-2 py-1 rounded-lg text-muted hover:text-accent transition-colors"
               title="決定事項"

```


## Codex レビュー

- [優先度: 今すぐ] `src/components/sidebar.tsx:690` / `:916` の `prefetch` 削除だけでは、Next.js の `Link` はデフォルトで prefetch が有効なため、パフォーマンス目的の変更としては効きません。意図が「ワークスペース一覧やサイドバーリンクの自動 prefetch を止める」なら、`prefetch={false}` を明示してください。


## 影響分析 (Claude read-only)

- **何が変わるか**: サイドバーのワークスペース切替リンクと「決定事項」ボタンに付いていた「先読み有効」の指定を外しました。ただしレビュアー指摘の通り Next.js のリンクは元から先読みが有効なため、見た目の動きはほぼ変わらず、本当に先読みを止めたい場合は別の書き方が必要です。
- **影響範囲**: 変更は sidebar.tsx の中の Link コンポーネント2か所の属性削除のみです。grep で "prefetch" を src 配下で検索したところ、ヒットは sidebar.tsx の他2か所（1107行・1907行）だけで、他ファイルからの参照はありません。エクスポートしている関数や型の変更でもないため、他画面（チャンネル画面・DM・ダッシュボード等）には波及しません。
- **LEVEL**: 影響なし
- **根拠**: 同一ファイル内の Link 属性を消すだけで、外部参照される API・関数・型に変更がない。


## ステータス

ユーザーが『スキップ』選択
