# src/app/(workspace)/[workspace]/[channel]/components/channel-view.tsx (2026-05-11T20:24:05)

**種別**: Git diff (vs HEAD)


## 変更前 diff

```
diff --git a/src/app/(workspace)/[workspace]/[channel]/components/channel-view.tsx b/src/app/(workspace)/[workspace]/[channel]/components/channel-view.tsx
index b77f6e6..25eaeec 100644
--- a/src/app/(workspace)/[workspace]/[channel]/components/channel-view.tsx
+++ b/src/app/(workspace)/[workspace]/[channel]/components/channel-view.tsx
@@ -139,6 +139,34 @@ export function ChannelView({ channel, initialMessages, currentUserId, initialLa
   // メッセージ本文の @<name> を厳密にマッチさせるために使う
   const [workspaceMemberNames, setWorkspaceMemberNames] = useState<string[]>([]);
 
+  // DM 相手の表示名 (channel_members 由来)。
+  // 既存ヘッダは messages から相手を探していたが、新規 DM (メッセージ 0 件) では
+  // 取れずに channel.name (= "dm") が出てしまうため、ここで別途取得する。
+  const [dmOtherName, setDmOtherName] = useState<string | null>(null);
+
+  useEffect(() => {
+    if (!channel.is_dm) {
+      setDmOtherName(null);
+      return;
+    }
+    let cancelled = false;
+    (async () => {
+      const { data } = await supabase
+        .from("channel_members")
+        .select("user_id, profiles(display_name)")
+        .eq("channel_id", channel.id);
+      if (cancelled || !data) return;
+      const other = (data as Array<{
+        user_id: string;
+        profiles: { display_name: string } | Array<{ display_name: string }> | null;
+      }>).find((m) => m.user_id !== currentUserId);
+      const p = other?.profiles;
+      const profile = Array.isArray(p) ? p[0] : p;
+      if (profile?.display_name) setDmOtherName(profile.display_name);
+    })();
+    return () => { cancelled = true; };
+  }, [channel.id, channel.is_dm, currentUserId, supabase]);
+
   useEffect(() => {
     let cancelled = false;
     async function fetchReadTimes() {

```


## Codex レビュー

- [優先度: 今すぐ] `useEffect` 内で DM チャンネルに入った直後に `dmOtherName` を `null` に戻していないため、DM A から DM B に遷移した際、取得完了まで前の相手名が表示される可能性があります。`if (channel.is_dm)` の fetch 前に `setDmOtherName(null)` し、`other` が見つからない場合も `null` をセットしてください。

- [優先度: 今すぐ] `data` が空、Supabase エラー、相手プロフィール未取得の場合に状態更新せず return しているため、古い `dmOtherName` が残ります。`const { data, error }` を受け取り、`error || !data` の場合も `setDmOtherName(null)` に倒すのが安全です。

- [優先度: 後で] `profiles` の型を `Array | object` として都度正規化していますが、Supabase の返却形に依存したキャストがコンポーネント内に埋まっています。`normalizeProfileDisplayName()` のような小さな helper に切り出すと、返却形変更時の修正箇所が明確になります。

- [優先度: 後で] テストに「メッセージ 0 件の DM で channel.name ではなく相手 display_name が表示される」「DM A から DM B へ遷移して古い相手名が残らない」を追加してください。今回の修正目的と副作用を直接カバーできます。


## Claude Code 修正 (応答)

採用: 「今すぐ」2件（fetch 前のクリア / error・未取得時の `null` フォールバック）。

不採用:
- helper 切り出し: この1箇所だけの正規化なので過剰抽象化、現状コメント込みで意図は読める。
- テスト追加: 既存のテスト基盤やパターン未確認のため、Author 判断に委ねる範囲。

変更: DM channel への遷移時に `setDmOtherName(null)` を先に実行し、`error || !data` および相手未検出も `null` セットに統一しました。


## 修正後 diff (vs HEAD)

```
diff --git a/src/app/(workspace)/[workspace]/[channel]/components/channel-view.tsx b/src/app/(workspace)/[workspace]/[channel]/components/channel-view.tsx
index 4ba360a..82cfbbc 100644
--- a/src/app/(workspace)/[workspace]/[channel]/components/channel-view.tsx
+++ b/src/app/(workspace)/[workspace]/[channel]/components/channel-view.tsx
@@ -149,20 +149,26 @@ export function ChannelView({ channel, initialMessages, currentUserId, initialLa
       setDmOtherName(null);
       return;
     }
+    // DM A → DM B 遷移時に前の相手名が残らないよう、fetch 前にクリアする
+    setDmOtherName(null);
     let cancelled = false;
     (async () => {
-      const { data } = await supabase
+      const { data, error } = await supabase
         .from("channel_members")
         .select("user_id, profiles(display_name)")
         .eq("channel_id", channel.id);
-      if (cancelled || !data) return;
+      if (cancelled) return;
+      if (error || !data) {
+        setDmOtherName(null);
+        return;
+      }
       const other = (data as Array<{
         user_id: string;
         profiles: { display_name: string } | Array<{ display_name: string }> | null;
       }>).find((m) => m.user_id !== currentUserId);
       const p = other?.profiles;
       const profile = Array.isArray(p) ? p[0] : p;
-      if (profile?.display_name) setDmOtherName(profile.display_name);
+      setDmOtherName(profile?.display_name ?? null);
     })();
     return () => { cancelled = true; };
   }, [channel.id, channel.is_dm, currentUserId, supabase]);

```


## ステータス

✅ 修正適用完了
