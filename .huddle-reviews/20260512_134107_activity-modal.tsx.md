# src/components/activity-modal.tsx (2026-05-12T13:41:10)

**種別**: Git diff (vs HEAD)


## 変更前 diff

```
diff --git a/src/components/activity-modal.tsx b/src/components/activity-modal.tsx
index 6c8cbc2..11edce0 100644
--- a/src/components/activity-modal.tsx
+++ b/src/components/activity-modal.tsx
@@ -46,6 +46,7 @@ type ReplyActivity = {
   channel_name: string;
   channel_slug: string;
   is_new: boolean;
+  is_hitorigoto: boolean;
 };
 
 type Tab = "reactions" | "mentions" | "replies";
@@ -379,7 +380,7 @@ export function ActivityModal({ workspaceSlug, workspaceId, currentChannelSlug,
               {replies.map((a) => (
                 <li key={a.reply_id}>
                   <button
-                    onClick={() => handleJump(a.channel_slug, a.reply_id)}
+                    onClick={() => handleJump(a.channel_slug, a.is_hitorigoto ? a.parent_message_id : a.reply_id)}
                     className={`w-full text-left flex items-start gap-3 px-4 py-3 transition-colors ${
                       a.is_new
                         ? "bg-[#eff6ff] hover:bg-[#dbeafe]"

```


## Codex レビュー

- [優先度: 今すぐ] `a.is_hitorigoto ? a.parent_message_id : a.reply_id` で `parent_message_id` が `null/undefined` の場合に壊れます。`ReplyActivity` の型で `parent_message_id` が必須か確認し、任意なら `a.parent_message_id ?? a.reply_id` のようにフォールバックしてください。

- [優先度: 今すぐ] `is_hitorigoto` を `ReplyActivity` の必須フィールドに追加していますが、既存APIレスポンスやモック/テストデータが未更新だと実行時に `undefined` 扱いになります。レスポンス生成側・fixture・テストデータも同時に更新してください。

- [優先度: 後で] 分岐ロジックを JSX 内に直書きすると意図が読みづらいです。例: `const targetMessageId = a.is_hitorigoto ? (a.parent_message_id ?? a.reply_id) : a.reply_id;` のように map 内で変数化すると、独り言では親メッセージへ飛ばす意図が明確になります。

- [優先度: 後で] リプライ一覧クリック時に「通常リプライは返信へ」「独り言は親へ」ジャンプする挙動のテストが必要です。`handleJump` に渡される message id を検証するコンポーネントテストを追加してください。


## 影響分析 (Claude read-only)

- **何が変わるか**: アクティビティ画面の「返信」一覧をクリックしたとき、独り言チャンネル宛の返信は元の独り言投稿に飛ぶように直します。独り言は「返信」がDOM上に存在しないので、これまでは飛んでも該当投稿が見つからずジャンプが失敗していました。
- **影響範囲**: `ReplyActivity` 型は activity-modal.tsx 内のローカル型で外部 export されておらず、grep でも同ファイルのみ参照していました。RPC `get_my_replies` は migration 119 で既に `is_hitorigoto` 列と `parent_message_id`(parent.id を内部結合) を返すよう更新済みで、両カラムとも NULL になりえません(parent を INNER JOIN しているため)。他ファイル(channel-view.tsx, sidebar-loader.tsx 等)の `is_hitorigoto` 参照は別系統のロジックで、今回の変更とは独立しています。
- **LEVEL**: 軽微
- **根拠**: ReplyActivity 型と handleJump 呼び出しは activity-modal.tsx 内で完結し、RPC 側スキーマも既に整合済み。


## ステータス

ユーザーが『スキップ』選択
