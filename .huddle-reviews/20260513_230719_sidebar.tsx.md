# src/components/sidebar.tsx (2026-05-13T23:07:22)

**種別**: Git diff (vs HEAD)


## 変更前 diff

```
diff --git a/src/components/sidebar.tsx b/src/components/sidebar.tsx
index 2d38d4a..be8e04d 100644
--- a/src/components/sidebar.tsx
+++ b/src/components/sidebar.tsx
@@ -155,7 +155,7 @@ export function Sidebar({
   // zustand セレクタ形式で購読範囲を限定（不要な再レンダーを防ぐ）
   const sidebarOpen = useMobileNavStore((s) => s.sidebarOpen);
   const setSidebarOpen = useMobileNavStore((s) => s.setSidebarOpen);
-  const setPendingDetailOpen = useMobileNavStore((s) => s.setPendingDetailOpen);
+  const startDetailOpen = useMobileNavStore((s) => s.startDetailOpen);
   const [searchQuery, setSearchQuery] = useState("");
 
   // プロフィール編集用のstate
@@ -838,7 +838,7 @@ export function Sidebar({
                   if (pathname === `/${workspaceSlug}/${hitorigotoChannel.slug}`) {
                     setSidebarOpen(false);
                   } else {
-                    setPendingDetailOpen(true);
+                    startDetailOpen("独り言");
                   }
                 }}
                 className="lg:hidden relative shrink-0 mr-2"
@@ -1037,9 +1037,9 @@ export function Sidebar({
             workspaceSlug={workspaceSlug}
             pathname={pathname}
             unreadState={unreadState}
-            onNavigate={(isActive) => {
+            onNavigate={(isActive, title) => {
               if (isActive) setSidebarOpen(false);
-              else setPendingDetailOpen(true);
+              else startDetailOpen(title);
             }}
             channelMembersMap={channelMembersMap}
             workspaceMembers={members}
@@ -1134,7 +1134,7 @@ export function Sidebar({
                 href={href}
                 onClick={() => {
                   if (isActive) setSidebarOpen(false);
-                  else setPendingDetailOpen(true);
+                  else startDetailOpen(name);
                 }}
                 className={`
                   flex items-center gap-2 px-3 py-2 text-base rounded-xl mx-2 transition-colors
@@ -1773,7 +1773,7 @@ type ChannelCategoryListProps = {
   workspaceSlug: string;
   pathname: string;
   unreadState: Record<string, number>;
-  onNavigate: (isActive: boolean) => void;
+  onNavigate: (isActive: boolean, title: string) => void;
   // channel.id → 所属ユーザーID配列
   channelMembersMap: Record<string, string[]>;
   // ユーザーID → プロフィール解決用
@@ -1939,7 +1939,7 @@ function ChannelCategoryList({
                   >
                     <Link
                       href={href}
-                      onClick={() => onNavigate(isActive)}
+                      onClick={() => onNavigate(isActive, channel.name)}
                       className={`flex items-center min-w-0 flex-1 px-3 py-2 text-base ${
                         isActive
                           ? "text-accent"

```


## Codex レビュー

- [優先度: 今すぐ] `startDetailOpen(title)` に渡す `title` が空文字や未定義相当にならない保証を入れてください。`channel.name` / `name` / `"独り言"` は見えていますが、型上 nullable の可能性がある場合は `startDetailOpen(title ?? "")` ではなく、表示名生成箇所で必ず非空文字に正規化してから渡すのが安全です。

- [優先度: 今すぐ] `ChannelCategoryListProps.onNavigate` のシグネチャ変更に伴い、他の呼び出し元が存在する場合は全て更新してください。`ChannelCategoryList` がこのファイル内専用でないなら、`rg "onNavigate={(isActive"` や型エラーで漏れを確認してください。

- [優先度: 後で] `startDetailOpen("独り言")` のような UI 表示文字列を状態遷移の入力に使う設計は、将来の文言変更や i18n で壊れやすいです。可能なら `startDetailOpen({ type: "channel", title })` や `startDetailOpen(channel.id)` のように、表示名と遷移対象の識別子を分けてください。

- [優先度: 後で] 追加された挙動はモバイルナビの状態遷移なので、最低限 `非アクティブなチャンネル/DMクリック時に startDetailOpen がタイトル付きで呼ばれる`、`アクティブ時は sidebar を閉じるだけ` のテストを追加してください。


## 影響分析 (Claude read-only)

- **何が変わるか**: モバイルでサイドバーから別チャンネルやDMを開くとき、画面遷移中に「いまどこへ向かっているか」のタイトル（チャンネル名やDM相手名、独り言など）を裏側で持てるようにします。これにより遷移アニメーション中のヘッダー表示などをより正確にできる土台ができます。
- **影響範囲**: `ChannelCategoryList` と `onNavigate` は sidebar.tsx 内のローカル定義で、grep しても他ファイルから参照されていません。`setPendingDetailOpen` は今もストアに残っており channel-view.tsx で使い続けているので互換性は保たれます。新たに使う `startDetailOpen` も sidebar.tsx 1ヶ所のみの呼び出しです。
- **LEVEL**: 軽微
- **根拠**: 変更範囲は sidebar.tsx 内のローカル関数と新規ストアアクション呼び出しに限定され、外部参照は無い。


## ステータス

ユーザーが『スキップ』選択
