# src/app/(workspace)/[workspace]/albums/page.tsx (2026-05-18T14:15:04)

**種別**: ファイル全体


## 変更前 diff

```
"use client";

import { useEffect, useState, useCallback } from "react";
import { useParams } from "next/navigation";
import { createClient } from "@/lib/supabase/client";
import { useMobileNavStore } from "@/stores/mobile-nav-store";
import type { AlbumSummary } from "@/lib/supabase/types";
import { AlbumCard } from "./components/album-card";
import { AlbumDetailModal } from "./components/album-detail-modal";
import { CreateAlbumModal } from "./components/create-album-modal";

export default function AlbumsPage() {
  const params = useParams<{ workspace: string }>();
  const workspaceSlug = params.workspace;
  const supabase = createClient();
  const setSidebarOpen = useMobileNavStore((s) => s.setSidebarOpen);

  const [workspaceId, setWorkspaceId] = useState<string | null>(null);
  const [currentUserId, setCurrentUserId] = useState<string | null>(null);
  const [albums, setAlbums] = useState<AlbumSummary[]>([]);
  const [channels, setChannels] = useState<Array<{ id: string; name: string; slug: string }>>([]);
  const [loading, setLoading] = useState(true);
  const [selectedAlbum, setSelectedAlbum] = useState<AlbumSummary | null>(null);
  const [showCreate, setShowCreate] = useState(false);
  const [addToAlbumId, setAddToAlbumId] = useState<string | null>(null);

  useEffect(() => {
    setSidebarOpen(false);
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, []);

  const fetchAlbums = useCallback(async () => {
    if (!workspaceId || !currentUserId) return;
    const { data } = await supabase.rpc("get_my_albums", {
      p_workspace_id: workspaceId,
      p_user_id: currentUserId,
    });
    if (data && Array.isArray(data)) {
      setAlbums(data as AlbumSummary[]);
    }
  }, [workspaceId, currentUserId, supabase]);

  useEffect(() => {
    let cancelled = false;
    async function init() {
      const { data: { user } } = await supabase.auth.getUser();
      if (!user || cancelled) return;
      setCurrentUserId(user.id);

      const { data: ws } = await supabase
        .from("workspaces")
        .select("id")
        .eq("slug", workspaceSlug)
        .maybeSingle();
      if (!ws || cancelled) return;
      setWorkspaceId(ws.id);

      // 自分が参加しているチャンネル一覧（アルバム作成用）
      const { data: chData } = await supabase
        .from("channel_members")
        .select("channels(id, name, slug)")
        .eq("user_id", user.id);
      if (chData && !cancelled) {
        const chs = chData
          .map((r) => {
            const ch = r.channels as unknown as { id: string; name: string; slug: string } | null;
            return ch;
          })
          .filter((ch): ch is { id: string; name: string; slug: string } => !!ch);
        setChannels(chs);
      }

      // アルバム一覧
      const { data: albumData } = await supabase.rpc("get_my_albums", {
        p_workspace_id: ws.id,
        p_user_id: user.id,
      });
      if (albumData && Array.isArray(albumData) && !cancelled) {
        setAlbums(albumData as AlbumSummary[]);
      }
      setLoading(false);
    }
    init();
    return () => { cancelled = true; };
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [workspaceSlug]);

  if (loading) {
    return (
      <div className="flex h-full items-center justify-center">
        <div className="w-5 h-5 border-2 border-accent border-t-transparent rounded-full animate-spin" />
      </div>
    );
  }

  return (
    <div className="flex flex-col h-full">
      {/* ヘッダー */}
      <header className="flex items-center justify-between px-4 py-3 border-b border-border bg-header shrink-0">
        <div className="flex items-center gap-2">
          <span className="text-lg">📸</span>
          <h1 className="text-base font-bold text-foreground">アルバム</h1>
          {albums.length > 0 && (
            <span className="text-xs bg-accent/10 text-accent rounded-full px-2 py-0.5">
              {albums.length}
            </span>
          )}
        </div>
        <button
          onClick={() => setShowCreate(true)}
          className="text-xs bg-accent text-white px-3 py-1.5 rounded-lg hover:bg-accent-hover transition-colors"
        >
          新規作成
        </button>
      </header>

      {/* アルバム一覧 */}
      <div className="flex-1 overflow-y-auto p-4">
        {albums.length === 0 ? (
          <div className="flex items-center justify-center h-full text-muted">
            <div className="text-center">
              <div className="text-5xl mb-4">📸</div>
              <p className="text-base font-medium text-foreground mb-2">アルバム</p>
              <p className="text-sm text-muted mb-4">
                イベントや行事の写真をアルバムにまとめましょう
              </p>
              <button
                onClick={() => setShowCreate(true)}
                className="bg-accent text-white px-4 py-2 rounded-xl text-sm font-medium hover:bg-accent-hover transition-colors"
              >
                アルバムを作成
              </button>
            </div>
          </div>
        ) : (
          <div className="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 gap-3">
            {albums.map((album) => (
              <AlbumCard
                key={album.id}
                album={album}
                onClick={() => setSelectedAlbum(album)}
              />
            ))}
          </div>
        )}
      </div>

      {/* 詳細モーダル */}
      {selectedAlbum && currentUserId && (
        <AlbumDetailModal
          album={selectedAlbum}
          currentUserId={currentUserId}
          onClose={() => setSelectedAlbum(null)}
          onAddItems={() => {
            setAddToAlbumId(selectedAlbum.id);
            setSelectedAlbum(null);
          }}
        />
      )}

      {/* 作成モーダル */}
      {showCreate && currentUserId && workspaceId && (
        <CreateAlbumModal
          workspaceId={workspaceId}
          currentUserId={currentUserId}
          channels={channels}
          onClose={() => setShowCreate(false)}
          onCreated={fetchAlbums}
        />
      )}

      {/* 既存アルバムに追加 */}
      {addToAlbumId && currentUserId && workspaceId && (
        <CreateAlbumModal
          workspaceId={workspaceId}
          currentUserId={currentUserId}
          channels={channels}
          addToAlbumId={addToAlbumId}
          onClose={() => setAddToAlbumId(null)}
          onCreated={fetchAlbums}
        />
      )}
    </div>
  );
}

```


## Codex レビュー

- [優先度: 今すぐ] `init()` 内で `!user` / `!ws` の場合に `return` しているため、`loading` が永久に `true` のままになります。`try/finally` にして、未ログイン・workspace未存在・RPC失敗時も `setLoading(false)` されるようにしてください。

- [優先度: 今すぐ] チャンネル取得が `.eq("user_id", user.id)` だけなので、ユーザーが参加している別workspaceのチャンネルも `CreateAlbumModal` に渡る可能性があります。`channels` を `workspace_id = ws.id` で絞る、または workspace 内の membership 経由で取得してください。

- [優先度: 後で] Supabase API の `error` をすべて無視しているため、失敗時に空表示・スピナー継続・古い一覧のままになる可能性があります。`auth.getUser()` / workspace取得 / channel取得 / `get_my_albums` の `error` を処理し、最低限 `albums` を空にするかエラー表示状態を持たせてください。

- [優先度: 後で] `workspaceSlug` が変わったときに `loading` や `albums` / `channels` / `selectedAlbum` がリセットされず、前workspaceの内容が一瞬残ります。`init()` 開始時に `setLoading(true)`, `setAlbums([])`, `setChannels([])`, `setSelectedAlbum(null)` を入れると安全です。

- [優先度: 後で] 上記の回帰を防ぐテストが不足しそうです。特に「未ログイン時にスピナーが止まる」「workspace未存在時にスピナーが止まる」「別workspaceのチャンネルが作成モーダルに出ない」「RPCが空配列を返したら空状態になる」を追加してください。


## 影響分析 (Claude read-only)

- **何が変わるか**: アルバム一覧ページで「ぐるぐる読み込み中マークが永遠に止まらない」不具合を直します。ログインしていない時やワークスペースが見つからない時でもちゃんと画面が表示されるようになります。また、別のグループの写真フォルダ候補が混ざって表示される問題も防ぎます。
- **影響範囲**: このページ(アルバム一覧画面)だけの修正です。grep で確認したところ、ページ本体は他のどこからも呼ばれていない独立画面で、使っている3つの部品(アルバムカード・詳細画面・作成画面)もすべてこのアルバム機能フォルダ内だけで使われています。チャットや他の画面への波及はありません。
- **LEVEL**: 影響なし
- **根拠**: ページは独立した画面で外部参照ゼロ、部品も同フォルダ内専用で受け渡し方法も変えていないため。


## Claude Code 修正 (応答)

採用した指摘と対応:

- **[今すぐ] loading が永久 true**: `try/finally` 化。未ログイン・workspace未存在・例外時も `setLoading(false)` される（`cancelled` 時は除く）。
- **[今すぐ] 別workspaceのチャンネル混入**: `channels` に `workspace_id` も取得し、`ch.workspace_id === ws.id` で絞り込み（モーダルへは `id/name/slug` のみ渡す）。
- **[後で] state リセット**: `init()` 冒頭で `loading=true / albums=[] / channels=[] / selectedAlbum=null` をリセットし、workspace 切替時の残像を防止。
- **[後で] エラー処理**: RPC 失敗・null 時は `albums` を空配列にフォールバック（スピナーは finally で必ず止まる）。

不採用:
- **テスト追加**: このリポジトリにテスト基盤が見当たらず、ページ単体修正のスコープ外。回帰防止は本質的に妥当だが、別タスクとして扱うべきと判断。

要約:
1. init を try/finally 化し、未ログイン/workspace未存在/例外時もスピナーが必ず止まるよう修正。
2. チャンネル一覧を現在の workspace_id で絞り込み、別workspaceのチャンネル混入を防止。
3. workspace 切替時に albums/channels/selectedAlbum/loading をリセットし残像を解消。


## 修正後 diff (vs HEAD)

```
"use client";

import { useEffect, useState, useCallback } from "react";
import { useParams } from "next/navigation";
import { createClient } from "@/lib/supabase/client";
import { useMobileNavStore } from "@/stores/mobile-nav-store";
import type { AlbumSummary } from "@/lib/supabase/types";
import { AlbumCard } from "./components/album-card";
import { AlbumDetailModal } from "./components/album-detail-modal";
import { CreateAlbumModal } from "./components/create-album-modal";

export default function AlbumsPage() {
  const params = useParams<{ workspace: string }>();
  const workspaceSlug = params.workspace;
  const supabase = createClient();
  const setSidebarOpen = useMobileNavStore((s) => s.setSidebarOpen);

  const [workspaceId, setWorkspaceId] = useState<string | null>(null);
  const [currentUserId, setCurrentUserId] = useState<string | null>(null);
  const [albums, setAlbums] = useState<AlbumSummary[]>([]);
  const [channels, setChannels] = useState<Array<{ id: string; name: string; slug: string }>>([]);
  const [loading, setLoading] = useState(true);
  const [selectedAlbum, setSelectedAlbum] = useState<AlbumSummary | null>(null);
  const [showCreate, setShowCreate] = useState(false);
  const [addToAlbumId, setAddToAlbumId] = useState<string | null>(null);

  useEffect(() => {
    setSidebarOpen(false);
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, []);

  const fetchAlbums = useCallback(async () => {
    if (!workspaceId || !currentUserId) return;
    const { data } = await supabase.rpc("get_my_albums", {
      p_workspace_id: workspaceId,
      p_user_id: currentUserId,
    });
    if (data && Array.isArray(data)) {
      setAlbums(data as AlbumSummary[]);
    }
  }, [workspaceId, currentUserId, supabase]);

  useEffect(() => {
    let cancelled = false;
    async function init() {
      // workspace 切り替え時に前 workspace の内容が残らないようリセット
      setLoading(true);
      setAlbums([]);
      setChannels([]);
      setSelectedAlbum(null);

      try {
        const { data: { user } } = await supabase.auth.getUser();
        if (!user || cancelled) return;
        setCurrentUserId(user.id);

        const { data: ws } = await supabase
          .from("workspaces")
          .select("id")
          .eq("slug", workspaceSlug)
          .maybeSingle();
        if (!ws || cancelled) return;
        setWorkspaceId(ws.id);

        // 自分が参加しているチャンネル一覧（アルバム作成用）
        // この workspace のチャンネルだけに絞る
        const { data: chData } = await supabase
          .from("channel_members")
          .select("channels(id, name, slug, workspace_id)")
          .eq("user_id", user.id);
        if (chData && !cancelled) {
          const chs = chData
            .map((r) => {
              const ch = r.channels as unknown as
                | { id: string; name: string; slug: string; workspace_id: string }
                | null;
              return ch;
            })
            .filter(
              (ch): ch is { id: string; name: string; slug: string; workspace_id: string } =>
                !!ch && ch.workspace_id === ws.id
            )
            .map(({ id, name, slug }) => ({ id, name, slug }));
          setChannels(chs);
        }

        // アルバム一覧
        const { data: albumData } = await supabase.rpc("get_my_albums", {
          p_workspace_id: ws.id,
          p_user_id: user.id,
        });
        if (!cancelled) {
          setAlbums(
            albumData && Array.isArray(albumData) ? (albumData as AlbumSummary[]) : []
          );
        }
      } finally {
        if (!cancelled) setLoading(false);
      }
    }
    init();
    return () => { cancelled = true; };
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [workspaceSlug]);

  if (loading) {
    return (
      <div className="flex h-full items-center justify-center">
        <div className="w-5 h-5 border-2 border-accent border-t-transparent rounded-full animate-spin" />
      </div>
    );
  }

  return (
    <div className="flex flex-col h-full">
      {/* ヘッダー */}
      <header className="flex items-center justify-between px-4 py-3 border-b border-border bg-header shrink-0">
        <div className="flex items-center gap-2">
          <span className="text-lg">📸</span>
          <h1 className="text-base font-bold text-foreground">アルバム</h1>
          {albums.length > 0 && (
            <span className="text-xs bg-accent/10 text-accent rounded-full px-2 py-0.5">
              {albums.length}
            </span>
          )}
        </div>
        <button
          onClick={() => setShowCreate(true)}
          className="text-xs bg-accent text-white px-3 py-1.5 rounded-lg hover:bg-accent-hover transition-colors"
        >
          新規作成
        </button>
      </header>

      {/* アルバム一覧 */}
      <div className="flex-1 overflow-y-auto p-4">
        {albums.length === 0 ? (
          <div className="flex items-center justify-center h-full text-muted">
            <div className="text-center">
              <div className="text-5xl mb-4">📸</div>
              <p className="text-base font-medium text-foreground mb-2">アルバム</p>
              <p className="text-sm text-muted mb-4">
                イベントや行事の写真をアルバムにまとめましょう
              </p>
              <button
                onClick={() => setShowCreate(true)}
                className="bg-accent text-white px-4 py-2 rounded-xl text-sm font-medium hover:bg-accent-hover transition-colors"
              >
                アルバムを作成
              </button>
            </div>
          </div>
        ) : (
          <div className="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 gap-3">
            {albums.map((album) => (
              <AlbumCard
                key={album.id}
                album={album}
                onClick={() => setSelectedAlbum(album)}
              />
            ))}
          </div>
        )}
      </div>

      {/* 詳細モーダル */}
      {selectedAlbum && currentUserId && (
        <AlbumDetailModal
          album={selectedAlbum}
          currentUserId={currentUserId}
          onClose={() => setSelectedAlbum(null)}
          onAddItems={() => {
            setAddToAlbumId(selectedAlbum.id);
            setSelectedAlbum(null);
          }}
        />
      )}

      {/* 作成モーダル */}
      {showCreate && currentUserId && workspaceId && (
        <CreateAlbumModal
          workspaceId={workspaceId}
          currentUserId={currentUserId}
          channels={channels}
          onClose={() => setShowCreate(false)}
          onCreated={fetchAlbums}
        />
      )}

      {/* 既存アルバムに追加 */}
      {addToAlbumId && currentUserId && workspaceId && (
        <CreateAlbumModal
          workspaceId={workspaceId}
          currentUserId={currentUserId}
          channels={channels}
          addToAlbumId={addToAlbumId}
          onClose={() => setAddToAlbumId(null)}
          onCreated={fetchAlbums}
        />
      )}
    </div>
  );
}

```


## ステータス

✅ 修正適用完了
