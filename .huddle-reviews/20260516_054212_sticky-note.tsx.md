# src/app/(workspace)/[workspace]/board/components/sticky-note.tsx (2026-05-16T05:43:22)

**種別**: Git diff (vs HEAD)


## 変更前 diff

```
diff --git a/src/app/(workspace)/[workspace]/board/components/sticky-note.tsx b/src/app/(workspace)/[workspace]/board/components/sticky-note.tsx
index fd75f39..113046f 100644
--- a/src/app/(workspace)/[workspace]/board/components/sticky-note.tsx
+++ b/src/app/(workspace)/[workspace]/board/components/sticky-note.tsx
@@ -14,7 +14,7 @@ const CATEGORY_COLORS = [
   "bg-rose-200",
 ];
 
-function categoryColor(category: string | null): string {
+export function categoryColor(category: string | null): string {
   if (!category) return "bg-stone-200";
   let hash = 0;
   for (let i = 0; i < category.length; i++) {
@@ -23,58 +23,24 @@ function categoryColor(category: string | null): string {
   return CATEGORY_COLORS[Math.abs(hash) % CATEGORY_COLORS.length];
 }
 
-function relativeTime(dateStr: string): string {
-  const diff = Date.now() - new Date(dateStr).getTime();
-  const min = Math.floor(diff / 60000);
-  if (min < 1) return "たった今";
-  if (min < 60) return `${min}分前`;
-  const hr = Math.floor(min / 60);
-  if (hr < 24) return `${hr}時間前`;
-  return new Date(dateStr).toLocaleDateString("ja-JP", { month: "short", day: "numeric" });
-}
-
 type Props = {
   note: BoardNoteWithProfile;
 };
 
 export function StickyNote({ note }: Props) {
   const colorClass = categoryColor(note.category);
-  const displayName = note.profiles?.display_name || "メンバー";
-  const avatarUrl = note.profiles?.avatar_url;
+  const displayName = note.profiles?.display_name || "";
 
   return (
-    <div
-      className={`aspect-square w-full p-3 shadow-md transition-all duration-300 flex flex-col ${colorClass}`}
-      style={{ minHeight: 120 }}
-    >
+    <div className={`w-28 h-28 p-2 shadow-md flex flex-col ${colorClass}`}>
       {/* 内容 */}
-      <p className="text-base text-gray-800 whitespace-pre-wrap break-words leading-relaxed flex-1 font-medium">
+      <p className="text-xs text-gray-800 leading-tight flex-1 overflow-hidden line-clamp-5">
         {note.content}
       </p>
-
-      {/* 投稿者 + 時刻 */}
-      <div className="flex items-center gap-1.5 mt-auto pt-2">
-        {avatarUrl ? (
-          // eslint-disable-next-line @next/next/no-img-element
-          <img src={avatarUrl} alt="" className="w-4 h-4 rounded-full object-cover" />
-        ) : (
-          <div className="w-4 h-4 rounded-full bg-black/10 flex items-center justify-center">
-            <span className="text-[8px] font-bold text-black/40">
-              {displayName[0]?.toUpperCase()}
-            </span>
-          </div>
-        )}
-        <span className="text-[11px] text-black/40 truncate">{displayName}</span>
-        <span className="text-[10px] text-black/25 ml-auto shrink-0">{relativeTime(note.created_at)}</span>
+      {/* 投稿者 */}
+      <div className="mt-auto pt-1">
+        <span className="text-[9px] text-black/35 truncate block">{displayName}</span>
       </div>
-
-      {/* 分類中インジケーター */}
-      {!note.category && (
-        <div className="flex items-center gap-1 mt-1">
-          <span className="inline-block w-1.5 h-1.5 rounded-full bg-gray-400 animate-pulse" />
-          <span className="text-[9px] text-black/25">分類中...</span>
-        </div>
-      )}
     </div>
   );
 }

```


## Codex レビュー

- [優先度: 今すぐ] `note.content` に長いURLや空白なし文字列が入ると、`break-words` を削除したため付箋外へ横にはみ出す可能性があります。`<p>` に `whitespace-pre-wrap break-words` か `break-all` を戻してください。

- [優先度: 今すぐ] `displayName` のフォールバックが `""` になり、プロフィール未取得時に投稿者欄が空表示になります。`note.profiles?.display_name || "メンバー"` に戻すか、空なら投稿者欄ごと非表示にしてください。

- [優先度: 後で] ルート要素が `w-full aspect-square minHeight:120` から `w-28 h-28` 固定に変わっており、親グリッドやレスポンシブ幅に追従しなくなります。意図的な固定サイズでないなら `aspect-square w-full min-h-[120px]` 系に戻すか、サイズをprops/親側で制御できる形にしてください。


## 影響分析 (Claude read-only)

- **何が変わるか**: 付箋カードのデザインを小さくコンパクトにする変更です。文字を小さく・付箋サイズを固定にし、投稿時刻と「分類中…」の表示やアイコンを省きシンプルにします。あわせて付箋の色分けロジックをボード画面の他の部品からも使えるよう共有化します。
- **影響範囲**: 色分け関数 categoryColor を export 化していますが、これは隣の board-canvas.tsx が `import { StickyNote, categoryColor }` で実際に取り込み、カテゴリ絞り込みボタンの色付け（60行目）に使っています。つまり今回の export 化はその利用を成立させるための変更で、共有部品を触っています。StickyNote 自体はボード画面の board-canvas.tsx 1箇所からのみ使用。削除する relativeTime は付箋ファイル内専用で、他ファイルの同名関数（master画面・ひとりごとカード）は別実装のため無関係です。
- **LEVEL**: 注意
- **根拠**: categoryColor を別ファイル(board-canvas.tsx)が import 利用しており、共有関数化を伴うため。


## ステータス

ユーザーが『スキップ』選択
