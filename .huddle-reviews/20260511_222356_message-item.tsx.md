# src/app/(workspace)/[workspace]/[channel]/components/message-item.tsx (2026-05-11T22:25:15)

**種別**: Git diff (vs HEAD)


## 変更前 diff

```
diff --git a/src/app/(workspace)/[workspace]/[channel]/components/message-item.tsx b/src/app/(workspace)/[workspace]/[channel]/components/message-item.tsx
index 4313a12..c0c91d4 100644
--- a/src/app/(workspace)/[workspace]/[channel]/components/message-item.tsx
+++ b/src/app/(workspace)/[workspace]/[channel]/components/message-item.tsx
@@ -3,7 +3,7 @@
 import { memo, useState, useRef, useEffect, useMemo } from "react";
 import { usePathname } from "next/navigation";
 import type { MessageWithProfile, Reaction } from "@/lib/supabase/types";
-import { EmojiPicker } from "./emoji-picker";
+import { EmojiPicker, EMOJI_LIST } from "./emoji-picker";
 import { PollDisplay } from "./poll-display";
 import { EventDisplay } from "./event-display";
 import { ImageLightbox } from "@/components/image-lightbox";
@@ -322,15 +322,6 @@ function MessageContent({
   );
 }
 
-// モバイル絵文字ピッカー用の定数
-const QUICK_EMOJIS = [
-  { category: "よく使う", emojis: ["👍", "❤️", "😂", "🎉", "🔥", "👀", "💯", "✅"] },
-  { category: "表情", emojis: ["😊", "😄", "🤔", "😮", "😢", "😡", "🥳", "😎"] },
-  { category: "ジェスチャー", emojis: ["👏", "🙌", "🤝", "💪", "✌️", "🫡", "👋", "🙏"] },
-  { category: "記号", emojis: ["⭐", "💡", "📌", "🚀", "⚡", "🎯", "📝", "🔔"] },
-  { category: "テキスト", emojis: ["完了しました！", "了解しました！", "確認中です", "対応いたします", "ありがとうございます！", "お疲れ様です！"] },
-];
-
 // リアクションバッジコンポーネント
 // タップ: 同じ絵文字を追加/解除（Zoom方式）
 // 長押し: 誰がリアクションしたか表示
@@ -423,7 +414,7 @@ function ReactionBadges({
                   <div className="absolute inset-0 bg-black/40" />
                   <div className="relative w-full animate-slide-up" onClick={(e) => e.stopPropagation()}>
                     <div className="w-full rounded-t-2xl bg-sidebar border-t border-border shadow-xl p-4 pb-20">
-                      {QUICK_EMOJIS.map((group) => (
+                      {EMOJI_LIST.map((group) => (
                         <div key={group.category} className="mb-3">
                           <p className="text-[11px] text-muted font-medium mb-1.5">{group.category}</p>
                           {group.category === "テキスト" ? (
@@ -1273,7 +1264,7 @@ export const MessageItem = memo(function MessageItem({
           <div className="absolute inset-0 bg-black/40" />
           <div className="relative w-full animate-slide-up" onClick={(e) => e.stopPropagation()}>
             <div className="w-full rounded-t-2xl bg-sidebar border-t border-border shadow-xl p-4 pb-20">
-              {QUICK_EMOJIS.map((group) => (
+              {EMOJI_LIST.map((group) => (
                 <div key={group.category} className="mb-3">
                   <p className="text-[11px] text-muted font-medium mb-1.5">{group.category}</p>
                   {group.category === "テキスト" ? (

```


## Codex レビュー

- [優先度: 今すぐ] `EMOJI_LIST` の型/内容が `QUICK_EMOJIS` と完全互換か確認してください。特にモバイル側は `group.category === "テキスト"` で表示分岐しているため、`emoji-picker` 側のカテゴリ名が変わるとテキスト定型文が通常絵文字扱いになります。カテゴリ名文字列ではなく `type: "text" | "emoji"` のような明示フィールドで分岐する形に直すのが安全です。

- [優先度: 後で] `message-item.tsx` が `EmojiPicker` コンポーネントの内部データ構造に依存するようになっています。`EMOJI_LIST` がピッカー専用の実装詳細なら、共有用に `emoji-data.ts` などへ切り出して `EmojiPicker` と `message-item.tsx` の両方から import する方が責務が明確です。

- [優先度: 後で] `EMOJI_LIST` にモバイル表示へ不向きな大量データが含まれる場合、ボトムシートの描画量が増えます。モバイル用に従来の quick set を維持したい意図なら、`MOBILE_QUICK_EMOJI_LIST` のような別 export に分けてください。


## Claude Code 修正 (応答)

残件なし。短い修正なので TodoWrite は不要。

**採用**: 指摘1（カテゴリ文字列分岐の脆さ）— `EMOJI_LIST` に `kind: "text" | "emoji"` 判別フィールドを追加し、`emoji-picker.tsx` と `message-item.tsx` 両方の分岐を `group.kind === "text"` に変更。

**不採用**:
- 指摘2（`emoji-data.ts` 切り出し）: 現状 import 元は `message-item.tsx` 1箇所のみ。`emoji-picker.tsx` から re-export で十分、過剰分割。
- 指摘3（モバイル用別 export 維持）: 直前コミット 50b91c9 で `QUICK_EMOJIS` を `EMOJI_LIST` に統一する方針が確定しており逆行する。データ量も実用範囲内。

**変更サマリ**:
- `emoji-picker.tsx`: `EMOJI_LIST` に `EmojiGroup` 型と `kind` フィールドを追加、判定ロジックを `kind === "text"` に変更
- `message-item.tsx`: モバイルピッカー2箇所の分岐を `group.kind === "text"` に変更


## 修正後 diff (vs HEAD)

```
diff --git a/src/app/(workspace)/[workspace]/[channel]/components/message-item.tsx b/src/app/(workspace)/[workspace]/[channel]/components/message-item.tsx
index c0c91d4..25fef36 100644
--- a/src/app/(workspace)/[workspace]/[channel]/components/message-item.tsx
+++ b/src/app/(workspace)/[workspace]/[channel]/components/message-item.tsx
@@ -417,7 +417,7 @@ function ReactionBadges({
                       {EMOJI_LIST.map((group) => (
                         <div key={group.category} className="mb-3">
                           <p className="text-[11px] text-muted font-medium mb-1.5">{group.category}</p>
-                          {group.category === "テキスト" ? (
+                          {group.kind === "text" ? (
                             <div className="flex flex-wrap gap-1.5">
                               {group.emojis.map((emoji) => (
                                 <button
@@ -1267,7 +1267,7 @@ export const MessageItem = memo(function MessageItem({
               {EMOJI_LIST.map((group) => (
                 <div key={group.category} className="mb-3">
                   <p className="text-[11px] text-muted font-medium mb-1.5">{group.category}</p>
-                  {group.category === "テキスト" ? (
+                  {group.kind === "text" ? (
                     <div className="flex flex-wrap gap-1.5">
                       {group.emojis.map((emoji) => (
                         <button

```


## ステータス

✅ 修正適用完了
