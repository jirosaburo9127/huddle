# src/app/(workspace)/[workspace]/albums/components/album-card.tsx (2026-05-18T14:11:53)

**種別**: ファイル全体


## 変更前 diff

```
"use client";

import type { AlbumSummary } from "@/lib/supabase/types";

type Props = {
  album: AlbumSummary;
  onClick: () => void;
};

export function AlbumCard({ album, onClick }: Props) {
  const coverUrl = album.first_item_url || album.cover_url;

  return (
    <button
      onClick={onClick}
      className="text-left rounded-2xl overflow-hidden border border-border hover:border-accent/30 transition-all hover:shadow-lg group"
    >
      {/* カバー画像 */}
      <div className="aspect-[4/3] bg-border/20 relative overflow-hidden">
        {coverUrl ? (
          // eslint-disable-next-line @next/next/no-img-element
          <img
            src={coverUrl}
            alt={album.title}
            className="w-full h-full object-cover group-hover:scale-105 transition-transform duration-300"
            loading="lazy"
          />
        ) : (
          <div className="w-full h-full flex items-center justify-center">
            <span className="text-3xl">📷</span>
          </div>
        )}
        {/* 枚数バッジ */}
        <span className="absolute bottom-2 right-2 bg-black/60 text-white text-[11px] font-medium px-2 py-0.5 rounded-full">
          {album.item_count}枚
        </span>
      </div>
      {/* 情報 */}
      <div className="px-3 py-2.5">
        <h3 className="text-sm font-bold text-foreground truncate">{album.title}</h3>
        <div className="flex items-center gap-1.5 mt-1">
          <span className="text-[11px] text-accent">#{album.channel_name}</span>
          <span className="text-[10px] text-muted">·</span>
          <span className="text-[11px] text-muted">{album.creator_name}</span>
        </div>
        <span className="text-[10px] text-muted/60 mt-0.5 block">
          {new Date(album.created_at).toLocaleDateString("ja-JP", { year: "numeric", month: "short", day: "numeric" })}
        </span>
      </div>
    </button>
  );
}

```


## Codex レビュー

- [優先度: 今すぐ] `coverUrl` の優先順位が不自然です。`album.cover_url` が設定されていても `first_item_url` があれば必ずそちらが表示されます。意図が「明示カバー優先」なら `const coverUrl = album.cover_url || album.first_item_url;` に修正してください。

- [優先度: 今すぐ] `new Date(album.created_at).toLocaleDateString(...)` は `created_at` が不正値/null相当だと `Invalid Date` 表示になります。`created_at` を検証し、不正なら非表示またはフォールバック文字列にしてください。

- [優先度: 後で] `channel_name` / `creator_name` が長い場合、横幅の狭いカードでレイアウトが崩れる可能性があります。親 `div` に `min-w-0`、各 `span` に `truncate` などを付けて省略表示にしてください。

- [優先度: 後で] `<button>` に `type="button"` がありません。将来フォーム内で使われた場合に submit されるため、`type="button"` を明示してください。


## 影響分析 (Claude read-only)

- **何が変わるか**: アルバム一覧に表示されるカードの不具合を直します。手動で選んだ表紙画像があるのに別の画像が表示されてしまう問題と、日付が「Invalid Date」と壊れて表示される問題を修正します。長い名前でレイアウトが崩れる対策も入れます。
- **影響範囲**: AlbumCard コンポーネントはまだどこからも読み込まれていません（grep で AlbumCard / album-card / albums/components を検索したが、定義ファイル自身以外にヒットなし）。型 AlbumSummary も types.ts の定義とこのファイルでしか使われておらず、メッセージ・通知・サイドバー等の既存機能には一切影響しません。
- **LEVEL**: 影響なし
- **根拠**: AlbumCard は外部から import されておらず、まだ画面に組み込まれていない新規・未使用コンポーネントのため。


## ステータス

ユーザーが『スキップ』選択
