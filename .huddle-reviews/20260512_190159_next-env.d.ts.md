# next-env.d.ts (2026-05-12T19:02:09)

**種別**: ファイル全体


## 変更前 diff

```
/// <reference types="next" />
/// <reference types="next/image-types/global" />
import "./.next/dev/types/routes.d.ts";

// NOTE: This file should not be edited
// see https://nextjs.org/docs/app/api-reference/config/typescript for more information.

```


## Codex レビュー

- [優先度: 今すぐ] `import "./.next/dev/types/routes.d.ts";` は `.next` 配下の生成物に依存しており、クリーンな checkout や CI、`next dev` 実行前の `tsc --noEmit` で `Cannot find module` になり得ます。`next-env.d.ts` からは削除し、必要なら `tsconfig.json` の `include` に Next が推奨する生成型のパスを含めてください。

- [優先度: 後で] `next-env.d.ts` は自動生成ファイルなので、手動編集された import が再生成時に消える可能性があります。ルート型の設定はこのファイルではなく Next/TypeScript 設定側に寄せると保守しやすいです。


## 影響分析 (Claude read-only)

- **何が変わるか**: TypeScriptが「型情報ファイルが見つからない」というエラーで止まらないようにする、開発環境の足回りの調整です。アプリの動作・見た目・機能は何も変わりません。
- **影響範囲**: grep で `routes.d.ts` を参照しているファイルを探したところ、本体コードでは1件もヒットせず、tsconfig.json の include に `.next/dev/types/**/*.ts` が既に登録されている（25〜31行目）ことを確認しました。つまりこの import 行を削除しても型解決は tsconfig 側でカバーされており、画面・機能・他コンポーネントへの影響はありません。
- **LEVEL**: 影響なし
- **根拠**: 該当行は外部から参照されておらず、必要な型は tsconfig.json の include 経由で既に読み込まれている。


## ステータス

ユーザーが『スキップ』選択
