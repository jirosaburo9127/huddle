# src/app/(workspace)/[workspace]/board/components/sticky-note.tsx (2026-05-16T05:48:46)

**種別**: Git diff (vs HEAD)


## 変更前 diff

```
diff --git a/src/app/(workspace)/[workspace]/board/components/sticky-note.tsx b/src/app/(workspace)/[workspace]/board/components/sticky-note.tsx
index 113046f..804a124 100644
--- a/src/app/(workspace)/[workspace]/board/components/sticky-note.tsx
+++ b/src/app/(workspace)/[workspace]/board/components/sticky-note.tsx
@@ -34,7 +34,7 @@ export function StickyNote({ note }: Props) {
   return (
     <div className={`w-28 h-28 p-2 shadow-md flex flex-col ${colorClass}`}>
       {/* 内容 */}
-      <p className="text-xs text-gray-800 leading-tight flex-1 overflow-hidden line-clamp-5">
+      <p className="text-sm font-bold text-gray-800 leading-tight flex-1 overflow-hidden line-clamp-4">
         {note.content}
       </p>
       {/* 投稿者 */}

```


## Codex レビュー

- [優先度: 後で] `sticky-note.tsx` の本文 `<p>` に `break-words` または `break-all` を追加してください。`text-sm font-bold` 化で長いURL/連続文字列がより横方向に切れやすくなるため、固定幅 `w-28` 内で折り返せるようにすると安全です。

- [優先度: 任意] 長文メモの表示確認を追加してください。`line-clamp-5` から `line-clamp-4` に減って表示量が変わるため、長文・投稿者名ありのケースを Storybook や簡単な表示テストで確認できると回帰に気づきやすいです。


## 影響分析 (Claude read-only)

- **何が変わるか**: 付箋ボードのメモの文字を少し大きく・太字にして読みやすくします。代わりに1枚に表示できる行数が5行から4行に減ります。
- **影響範囲**: この付箋部品(StickyNote)を使っているのはボード画面の board-canvas.tsx 1ファイルだけで(grep で確認)、今回の変更は文字の見た目クラスのみ。部品の名前・受け取るデータ・他に公開している categoryColor 関数は一切変えていないので、ボード画面以外(チャンネル/DM/設定など)には影響しません。
- **LEVEL**: 軽微
- **根拠**: 変更はStickyNote内の表示用CSSクラスのみで、export仕様も呼び出し側も不変のため。


## ステータス

ユーザーが『スキップ』選択
