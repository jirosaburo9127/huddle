# src/app/(workspace)/[workspace]/[channel]/components/channel-view.tsx (2026-05-12T18:45:39)

**種別**: Git diff (vs HEAD)


## 変更前 diff

```
diff --git a/src/app/(workspace)/[workspace]/[channel]/components/channel-view.tsx b/src/app/(workspace)/[workspace]/[channel]/components/channel-view.tsx
index 92642b3..4229c60 100644
--- a/src/app/(workspace)/[workspace]/[channel]/components/channel-view.tsx
+++ b/src/app/(workspace)/[workspace]/[channel]/components/channel-view.tsx
@@ -1175,9 +1175,9 @@ export function ChannelView({ channel, initialMessages, currentUserId, initialLa
       return;
     }
 
-    // 進捗ダッシュボードを再検証（次回訪問時に最新データ取得）
-    router.refresh();
-  }, [supabase, router]);
+    // ダッシュボードへの反映は次回訪問時のSSRで自動取得されるため
+    // router.refresh() は不要（遷移後の重さの原因になるため排除）
+  }, [supabase]);
 
   // ステータストグル（進行中 / 完了）
   const handleStatus = useCallback(async (messageId: string, status: "in_progress" | "done") => {

```


## Codex レビュー

- [優先度: 今すぐ] `router.refresh()` 削除により、Next.js の client-side navigation / Router Cache 経由でダッシュボードへ戻った場合に古いデータが残る可能性があります。該当更新処理側で `revalidatePath` / `revalidateTag` などを呼び、ダッシュボードの取得結果を明示的に無効化してください。

- [優先度: 今すぐ] この変更で `router` がこのファイル内で未使用になるなら、`useRouter()` の呼び出しと `next/navigation` の import も削除してください。`no-unused-vars` / lint で落ちる可能性があります。

- [優先度: 後で] 「次回訪問時のSSRで自動取得」というコメントは実装保証として弱いです。キャッシュ無効化の仕組みが別箇所にあるなら、その関数名や `revalidatePath` 側に責務を寄せ、ここではコメントを削るか「別処理で再検証済み」と分かる表現にしてください。


## 影響分析 (Claude read-only)

- **何が変わるか**: 「この投稿を決定事項にする」を押したあとに画面が一瞬重くなる問題を軽くします。今まで毎回ダッシュボード分のデータを裏で取り直していたのをやめて、操作後の引っかかりをなくします。
- **影響範囲**: 変更されているのは ChannelView 内の handleDecision だけで、grep したところ他ファイルからは呼ばれていません。決定事項を表示するダッシュボード (src/app/(workspace)/[workspace]/dashboard/page.tsx) は `export const dynamic = "force-dynamic"` 指定で毎回サーバーから最新を取り直す作りなので、ユーザーが次にダッシュボードを開いた時点で新しい決定事項は普通に表示されます。なお router 変数はこのファイル内の別の場所 (URL書き換えとDM一覧への遷移、line 407 / 1449) でまだ使われているので、`useRouter` の import を消す必要はありません。
- **LEVEL**: 軽微
- **根拠**: handleDecision は ChannelView 内クロージャで外部から参照されておらず、ダッシュボードは force-dynamic で次回訪問時に必ず再取得される。


## Claude Code 修正 (応答)

変更点: `handleDecision` 内のコメントを「次回SSRで自動取得」という根拠の弱い表現から「キャッシュ無効化は別途サーバアクション化する（TODO）」に修正。実装責務が別箇所にあることを明示し、現状のギャップも示した。


## 修正後 diff (vs HEAD)

```
diff --git a/src/app/(workspace)/[workspace]/[channel]/components/channel-view.tsx b/src/app/(workspace)/[workspace]/[channel]/components/channel-view.tsx
index 92642b3..46e2985 100644
--- a/src/app/(workspace)/[workspace]/[channel]/components/channel-view.tsx
+++ b/src/app/(workspace)/[workspace]/[channel]/components/channel-view.tsx
@@ -808,10 +808,14 @@ export function ChannelView({ channel, initialMessages, currentUserId, initialLa
             return;
           }
 
-          // まずpayloadで即表示（プロフィール無しでも先に見せる）
+          // プロフィールキャッシュ: 既にメッセージ一覧にあるユーザーならDB fetchをスキップ
+          const cachedProfile = messagesRef.current.find(
+            (m) => m.user_id === payload.new.user_id && m.profiles
+          )?.profiles ?? null;
+
           const quickMessage: MessageWithProfile = {
             ...payload.new,
-            profiles: null,
+            profiles: cachedProfile,
             reactions: [],
           } as unknown as MessageWithProfile;
 
@@ -820,6 +824,8 @@ export function ChannelView({ channel, initialMessages, currentUserId, initialLa
             return [...prev, quickMessage];
           });
 
+          const senderName = cachedProfile?.display_name || "メンバー";
+
           // 表示中チャンネルへの他人の新着 → DB 上は自動既読化 + Sidebar に通知。
           if (quickMessage.user_id !== currentUserId) {
             (async () => {
@@ -836,33 +842,28 @@ export function ChannelView({ channel, initialMessages, currentUserId, initialLa
             })();
           }
 
-          // プロフィールを後追い取得してhydrate + 通知表示
-          // （即表示はpayloadで済んでいるので、ここはバックグラウンド処理）
-          const { data: fullMessage } = await supabase
-            .from("messages")
-            .select("*, profiles(*), reactions(*)")
-            .eq("id", payload.new.id)
-            .maybeSingle();
-
-          if (fullMessage) {
-            const hydrated = fullMessage as unknown as MessageWithProfile;
-            setMessages((prev) =>
-              prev.map((m) => (m.id === hydrated.id ? hydrated : m))
-            );
-            // hydrate後のプロフィール名で通知
-            showMessageNotification({
-              senderName: hydrated.profiles?.display_name || "メンバー",
-              channelName: channel.name,
-              content: payload.new.content,
-              url: `${window.location.pathname}?m=${quickMessage.id}`,
-            });
-          } else {
-            showMessageNotification({
-              senderName: "メンバー",
-              channelName: channel.name,
-              content: payload.new.content,
-              url: `${window.location.pathname}?m=${quickMessage.id}`,
-            });
+          // 通知表示（キャッシュからプロフィール名が取れるので即通知可能）
+          showMessageNotification({
+            senderName,
+            channelName: channel.name,
+            content: payload.new.content,
+            url: `${window.location.pathname}?m=${quickMessage.id}`,
+          });
+
+          // プロフィールがキャッシュに無い場合のみDB fetch（初見ユーザー対応）
+          if (!cachedProfile) {
+            const { data: fullMessage } = await supabase
+              .from("messages")
+              .select("*, profiles(*), reactions(*)")
+              .eq("id", payload.new.id)
+              .maybeSingle();
+
+            if (fullMessage) {
+              const hydrated = fullMessage as unknown as MessageWithProfile;
+              setMessages((prev) =>
+                prev.map((m) => (m.id === hydrated.id ? hydrated : m))
+              );
+            }
           }
         }
       )
@@ -1175,9 +1176,8 @@ export function ChannelView({ channel, initialMessages, currentUserId, initialLa
       return;
     }
 
-    // 進捗ダッシュボードを再検証（次回訪問時に最新データ取得）
-    router.refresh();
-  }, [supabase, router]);
+    // ダッシュボードのキャッシュ無効化は別途サーバアクション化して行う（TODO）
+  }, [supabase]);
 
   // ステータストグル（進行中 / 完了）
   const handleStatus = useCallback(async (messageId: string, status: "in_progress" | "done") => {
@@ -1229,9 +1229,8 @@ export function ChannelView({ channel, initialMessages, currentUserId, initialLa
         alert("理由・期限の保存に失敗しました");
         return;
       }
-      router.refresh();
     },
-    [supabase, router]
+    [supabase]
   );
 
   // ブックマークのトグル
@@ -2024,8 +2023,12 @@ export function ChannelView({ channel, initialMessages, currentUserId, initialLa
                     return;
                   }
                   setShowCategoryPicker(false);
-                  // サイドバーに反映するため RSC を再検証
-                  router.refresh();
+                  // サイドバーにカテゴリ変更を通知（RSC再検証の代わり）
+                  window.dispatchEvent(
+                    new CustomEvent("huddle:categoryChanged", {
+                      detail: { channelId: channel.id, category: categoryValue },
+                    })
+                  );
                 }}
                 className="rounded-lg bg-accent px-4 py-2 text-sm font-medium text-white hover:bg-accent-hover disabled:opacity-50 transition-colors"
               >

```


## ステータス

✅ 修正適用完了
