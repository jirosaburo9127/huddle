# src/app/(workspace)/[workspace]/[channel]/components/channel-view.tsx (2026-05-12T13:39:39)

**種別**: Git diff (vs HEAD)


## 変更前 diff

```
diff --git a/src/app/(workspace)/[workspace]/[channel]/components/channel-view.tsx b/src/app/(workspace)/[workspace]/[channel]/components/channel-view.tsx
index bdac419..b856355 100644
--- a/src/app/(workspace)/[workspace]/[channel]/components/channel-view.tsx
+++ b/src/app/(workspace)/[workspace]/[channel]/components/channel-view.tsx
@@ -815,12 +815,12 @@ export function ChannelView({ channel, initialMessages, currentUserId, initialLa
             })();
           }
 
-          // 通知表示
+          // 通知表示（投稿IDを含めて、クリックで該当投稿にジャンプ可能にする）
           showMessageNotification({
             senderName: newMessage.profiles?.display_name || "メンバー",
             channelName: channel.name,
             content: payload.new.content,
-            url: window.location.pathname,
+            url: `${window.location.pathname}?m=${newMessage.id}`,
           });
         }
       )

```


## Codex レビュー

- [優先度: 今すぐ] `url: \`${window.location.pathname}?m=${newMessage.id}\`` は既存のクエリ文字列を破棄します。現在のURLに `?thread=...` などがあると壊れるため、`new URL(window.location.href)` と `searchParams.set("m", newMessage.id)` で既存パラメータを保持してください。

- [優先度: 後で] `newMessage.id` をテンプレート直挿ししています。ID形式がUUID等に固定されていても、URL生成としては `URLSearchParams` 経由にしてエンコードを任せる方が安全で読みやすいです。

- [優先度: 後で] この変更だけでは「クリックで該当投稿にジャンプ可能」かは保証されません。通知遷移後に `m` パラメータを読み、対象メッセージへスクロール/ハイライトする処理が未実装なら追加してください。既にあるなら、この通知URL生成に対するテストを追加してください。


## 影響分析 (Claude read-only)

- **何が変わるか**: ブラウザの新着通知をクリックしたとき、これまではチャンネルのトップに飛ぶだけだったのが、その通知の元になった投稿そのものまでジャンプできるようになります。
- **影響範囲**: 変更は channel-view.tsx 内のリアルタイム購読部分1か所だけです。通知を受け取って投稿ジャンプを実行する側 (src/lib/notification.ts) は既に `?m=<投稿ID>` を読んでジャンプする実装になっており、同じ形式のURLは sidebar.tsx、in-progress、files、dashboard-view、search、activity-modal などからも生成されているので、今回の変更は既存の挙動と整合しています。同一チャンネルなら CustomEvent でその場ジャンプ、別チャンネルなら通常遷移という分岐も notification.ts 側で対応済みです。
- **LEVEL**: 軽微
- **根拠**: 変更は同ファイル内の通知URL生成1行のみで、受け手側の `?m=` 処理は既存実装に対応済み。


## ステータス

ユーザーが『スキップ』選択
