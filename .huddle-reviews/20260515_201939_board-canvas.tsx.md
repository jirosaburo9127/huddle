# src/app/(workspace)/[workspace]/board/components/board-canvas.tsx (2026-05-15T20:20:35)

**種別**: ファイル全体


## 変更前 diff

```
"use client";

import { useMemo } from "react";
import type { BoardNoteWithProfile } from "@/lib/supabase/types";
import { StickyNote } from "./sticky-note";

type Props = {
  notes: BoardNoteWithProfile[];
};

export function BoardCanvas({ notes }: Props) {
  // カテゴリ別にグループ化（未分類は末尾）
  const grouped = useMemo(() => {
    const map = new Map<string, BoardNoteWithProfile[]>();
    const uncategorized: BoardNoteWithProfile[] = [];

    for (const note of notes) {
      if (!note.category) {
        uncategorized.push(note);
      } else {
        const list = map.get(note.category);
        if (list) {
          list.push(note);
        } else {
          map.set(note.category, [note]);
        }
      }
    }

    // カテゴリを投稿数の多い順にソート
    const sorted = [...map.entries()].sort((a, b) => b[1].length - a[1].length);
    if (uncategorized.length > 0) {
      sorted.push(["未分類", uncategorized]);
    }
    return sorted;
  }, [notes]);

  if (notes.length === 0) {
    return (
      <div className="flex-1 flex items-center justify-center text-muted px-6">
        <div className="text-center">
          <div className="text-4xl mb-3">📝</div>
          <p className="text-sm">まだ付箋がありません</p>
          <p className="text-xs mt-1 text-muted/70">チャットでアイディアを投稿してください</p>
        </div>
      </div>
    );
  }

  return (
    <div className="flex-1 overflow-y-auto p-4">
      <div className="grid grid-cols-1 md:grid-cols-2 xl:grid-cols-3 gap-4">
        {grouped.map(([category, categoryNotes]) => (
          <div key={category} className="space-y-2">
            {/* カテゴリヘッダ */}
            <div className="flex items-center gap-2 px-1">
              <h3 className="text-xs font-bold text-foreground/70 uppercase tracking-wider">
                {category}
              </h3>
              <span className="text-[10px] bg-foreground/10 text-foreground/50 rounded-full px-1.5 py-0.5">
                {categoryNotes.length}
              </span>
            </div>
            {/* 付箋リスト */}
            <div className="space-y-2">
              {categoryNotes.map((note) => (
                <StickyNote key={note.id} note={note} />
              ))}
            </div>
          </div>
        ))}
      </div>
    </div>
  );
}

```


## Codex レビュー

- [優先度: 今すぐ] `key={category}` は、実カテゴリに `"未分類"` が存在し、かつ `note.category` なしの付箋もある場合に重複します。`grouped` の要素を `{ key: "__uncategorized__", label: "未分類", notes }` のような形にして、表示ラベルと React key を分けてください。

- [優先度: 後で] `!note.category` だと空文字だけでなく想定外の falsy 値を未分類扱いします。型上 `string | null` なら `note.category == null || note.category.trim() === ""` のように意図を明示すると、空白カテゴリも吸収できます。

- [優先度: 後で] 同数カテゴリのソート順が `notes` の入力順に依存します。再取得や並び替えで表示が揺れやすいので、`b[1].length - a[1].length || a[0].localeCompare(b[0], "ja")` のようなタイブレークを入れると安定します。

- [優先度: 任意] グルーピングロジックは UI から切り出すとテストしやすいです。特に「未分類が末尾」「カテゴリ件数順」「実カテゴリの未分類との key 衝突なし」を単体テストで押さえてください。


## 影響分析 (Claude read-only)

- **何が変わるか**: 付箋ボードで「未分類」というカテゴリ名を実際に付けた付箋と、カテゴリ未設定の付箋が両方ある時に、画面表示が崩れる可能性があったバグを直します。あわせて、同じ件数のカテゴリの並び順が再読み込みのたびに入れ替わってチラつく問題も安定させます。
- **影響範囲**: この付箋の振り分け処理は board-canvas.tsx の中だけで完結しており、外から呼ばれる入り口（受け取るデータの形 = notes一覧）は変わりません。grep で BoardCanvas を検索したところ、使っているのは board-layout.tsx の1ファイルのみで、そこでの渡し方（notes一覧を渡すだけ）は今回変わらないため、ボード画面以外（チャンネル・DM等）には影響しません。
- **LEVEL**: 軽微
- **根拠**: 変更は BoardCanvas 内部のグループ化ロジックのみで、外部に公開している受け渡しの形は不変のため。


## ステータス

ユーザーが『スキップ』選択
