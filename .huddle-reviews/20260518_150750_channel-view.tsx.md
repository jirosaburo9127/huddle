# src/app/(workspace)/[workspace]/[channel]/components/channel-view.tsx (2026-05-18T15:08:19)

**種別**: Git diff (vs HEAD)


## 変更前 diff

```
diff --git a/src/app/(workspace)/[workspace]/[channel]/components/channel-view.tsx b/src/app/(workspace)/[workspace]/[channel]/components/channel-view.tsx
index 790481a..3c02745 100644
--- a/src/app/(workspace)/[workspace]/[channel]/components/channel-view.tsx
+++ b/src/app/(workspace)/[workspace]/[channel]/components/channel-view.tsx
@@ -16,6 +16,7 @@ import { EventDisplay } from "./event-display";
 import { DateSeparator } from "./date-separator";
 import { ChannelNote } from "./channel-note";
 import { ChannelMembersModal } from "@/components/channel-members-modal";
+import { ChannelAlbums } from "./channel-albums";
 import { useMobileNavStore } from "@/stores/mobile-nav-store";
 import { showMessageNotification } from "@/lib/notification";
 import { clearPushBadge } from "@/lib/push-notifications";
@@ -85,6 +86,7 @@ export function ChannelView({ channel, initialMessages, currentUserId, initialLa
   const [replyTo, setReplyTo] = useState<MessageWithProfile | null>(null);
   // 独り言チャンネル用X風スレッドモーダル
   const [showMembersModal, setShowMembersModal] = useState(false);
+  const [showAlbums, setShowAlbums] = useState(false);
   const [showDeleteChannel, setShowDeleteChannel] = useState(false);
   const [deletingChannel, setDeletingChannel] = useState(false);
   const [showDecisionsOnly, setShowDecisionsOnly] = useState(false);
@@ -1845,6 +1847,20 @@ export function ChannelView({ channel, initialMessages, currentUserId, initialLa
                       </svg>
                       メンバー管理
                     </button>
+
+                    <button
+                      role="menuitem"
+                      onClick={() => {
+                        setShowOverflowMenu(false);
+                        setShowAlbums(true);
+                      }}
+                      className="w-full flex items-center gap-2 px-3 py-2 text-sm text-foreground hover:bg-white/[0.04] transition-colors"
+                    >
+                      <svg className="w-4 h-4 shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24" strokeWidth={2}>
+                        <path strokeLinecap="round" strokeLinejoin="round" d="M2.25 15.75l5.159-5.159a2.25 2.25 0 013.182 0l5.159 5.159m-1.5-1.5l1.409-1.409a2.25 2.25 0 013.182 0l2.909 2.909M3.75 21h16.5A2.25 2.25 0 0022.5 18.75V5.25A2.25 2.25 0 0020.25 3H3.75A2.25 2.25 0 001.5 5.25v13.5A2.25 2.25 0 003.75 21z" />
+                      </svg>
+                      アルバム
+                    </button>
                   )}
 
                   {/* カテゴリ変更 (DMでは非表示) */}
@@ -2165,6 +2181,17 @@ export function ChannelView({ channel, initialMessages, currentUserId, initialLa
         />
       )}
 
+      {/* チャンネルアルバム */}
+      {showAlbums && (
+        <ChannelAlbums
+          channelId={channel.id}
+          channelName={channel.name}
+          workspaceId={channel.workspace_id}
+          currentUserId={currentUserId}
+          onClose={() => setShowAlbums(false)}
+        />
+      )}
+
       {/* チャンネルカテゴリ変更モーダル */}
       {showCategoryPicker && (
         <div

```


## Codex レビュー

- [優先度: 今すぐ] `ChannelAlbums` を静的 import しているため、アルバムを開かない通常表示でもバンドルに含まれます。モーダル系で重い可能性があるなら `next/dynamic` で遅延ロードしてください。例: `const ChannelAlbums = dynamic(() => import("./channel-albums").then(m => m.ChannelAlbums), { ssr: false });`

- [優先度: 今すぐ] アルバムメニューの表示条件が「メンバー管理」と同じブロック内に入っているように見えます。アルバム閲覧が管理権限不要の機能なら、`ChannelAlbums` ボタンは権限制御ブロックの外へ出してください。逆に管理者限定なら、`ChannelAlbums` 側でも同じ権限チェックが必要です。

- [優先度: 後で] SVG がインラインで追加されており、このファイルの肥大化を進めます。既存方針に合わせて icon component、または `lucide-react` 等の既存アイコンに置き換えると可読性が上がります。


## 影響分析 (Claude read-only)

- **何が変わるか**: チャンネルのメニューに「アルバム」ボタンが追加され、そのチャンネルの写真をまとめたアルバム画面を開けるようになります。
- **影響範囲**: 「ChannelAlbums」を grep したところ、新規部品 channel-albums.tsx と、この channel-view.tsx の2つでしか使われておらず、他のページや機能からは呼ばれていません。表示条件は「DM(個人間チャット)ではない」というだけで、レビュアーが心配した「管理者専用」の制限ではないため、チャンネルにいる人なら誰でもアルバムを開けます。既存の「メンバー管理」「写真・動画」ボタンの並びに1つ足すだけの追加です。
- **LEVEL**: 軽微
- **根拠**: ChannelAlbums は今回新設の部品で参照箇所はこのファイルのみ、既存機能の動作は変更していないため。


## ステータス

ユーザーが『スキップ』選択
