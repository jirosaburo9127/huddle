# src/components/sidebar.tsx (2026-05-12T13:40:19)

**種別**: Git diff (vs HEAD)


## 変更前 diff

```
diff --git a/src/components/sidebar.tsx b/src/components/sidebar.tsx
index 86c14a0..597db1e 100644
--- a/src/components/sidebar.tsx
+++ b/src/components/sidebar.tsx
@@ -595,7 +595,7 @@ export function Sidebar({
             senderName,
             channelName: channelLabel,
             content: msg.content,
-            url: `/${workspaceSlug}/${ch.slug}`,
+            url: `/${workspaceSlug}/${ch.slug}?m=${msg.id}`,
           });
         }
       )

```


## Codex レビュー

- [優先度: 今すぐ] `msg.id` はURLクエリに入るため、`encodeURIComponent(String(msg.id))` でエンコードしてください。ID形式が将来 `:` や `/` などを含むとリンクが壊れます。
- [優先度: 後で] このリンク先で `?m=` を処理する実装が未追加なら、該当メッセージへのスクロール・ハイライトが動くように受け側の処理とテストを追加してください。
- [優先度: 後で] 通知/未読一覧のリンク生成テストがあるなら、`/${workspaceSlug}/${ch.slug}?m=<id>` になるケースを追加してください。クエリ付きURLは回帰しやすいです。


## 影響分析 (Claude read-only)

- **何が変わるか**: ブラウザ通知をクリックしたときに、通知の対象になったメッセージへ直接ジャンプできるようになります。今までは通知をクリックしてもチャンネルの末尾を開くだけで、どのメッセージかが分かりにくい状態でした。
- **影響範囲**: sidebar.tsx 内の通知購読処理だけの変更で、生成された URL は同ファイル内の showMessageNotification にそのまま渡されるだけです。grep で `?m=` を検索したところ、受け側 (channel-view.tsx, push-tap-handler.tsx, activity-modal.tsx 等) ではすでに `?m=` パラメータでメッセージへジャンプする仕組みが実装されていたので、リンク先で正しく動作する想定です。
- **LEVEL**: 軽微
- **根拠**: 同コンポーネント内の通知URL文字列を1行差し替えただけで、`?m=` 受け側の処理は既存実装が対応済み。


## ステータス

ユーザーが『スキップ』選択
