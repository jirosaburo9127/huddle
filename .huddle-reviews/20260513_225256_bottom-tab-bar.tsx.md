# src/components/bottom-tab-bar.tsx (2026-05-13T22:53:57)

**種別**: Git diff (vs HEAD)


## 変更前 diff

```
diff --git a/src/components/bottom-tab-bar.tsx b/src/components/bottom-tab-bar.tsx
index 502f97f..fa3497a 100644
--- a/src/components/bottom-tab-bar.tsx
+++ b/src/components/bottom-tab-bar.tsx
@@ -24,11 +24,9 @@ export function BottomTabBar({ workspaceSlug, workspaceId, currentUserId, member
   const [showBookmark, setShowBookmark] = useState(false);
   const [showMembers, setShowMembers] = useState(false);
 
-  // URL が変わったらポップオーバーとサイドバーを閉じる。
-  // （iOS で "タップしても画面変わらない" と見えていたのは、実は navigation は
-  // 成功していて、開きっぱなしのサイドバーが新しいページの上を覆っていただけ）
-  // ただし初回マウント時は skip: ワークスペーストップ /ws-xxx でサイドバーを
-  // 自動表示する WorkspaceLobby の setSidebarOpen(true) を打ち消してしまうため
+  // URL が変わったらポップオーバーだけ閉じる。
+  // チャンネル遷移では ChannelView がマウントされたタイミングでサイドバーを閉じる。
+  // pathname 変更直後に閉じると loading だけが先に表示され、白く見えるため。
   const initialMountRef = useRef(true);
   // pathname 変化（外部のナビゲーション state）に同期して UI を閉じる。
   // 派生 state では計算できないため effect 内で setState する正当なケース
@@ -39,8 +37,7 @@ export function BottomTabBar({ workspaceSlug, workspaceId, currentUserId, member
     }
     // eslint-disable-next-line react-hooks/set-state-in-effect
     setShowMore(false);
-    setSidebarOpen(false);
-  }, [pathname, setSidebarOpen]);
+  }, [pathname]);
 
   // ポップオーバー外タップで閉じる（その他ボタン自身と中身は除外）
   useEffect(() => {

```


## Codex レビュー

- [優先度: 今すぐ] `setSidebarOpen` がこの effect 以外で使われていない場合、props / 引数に未使用変数が残ります。`BottomTabBar` の分割代入から `setSidebarOpen` を外すか、まだ他所で必要なら用途が分かる位置で使ってください。

- [優先度: 今すぐ] コメントでは「チャンネル遷移では ChannelView がマウントされたタイミングでサイドバーを閉じる」とありますが、このファイル単体ではその保証がありません。`ChannelView` 側で `useEffect(() => setSidebarOpen(false), [...])` 相当が実装済みか確認し、未実装なら追加してください。未対応だとチャンネル以外への遷移時にサイドバーが残る回帰になります。

- [優先度: 後で] `pathname` 変更時に `setShowBookmark(false)` / `setShowMembers(false)` も閉じる必要があるなら、今回の変更後もそれらが残り続ける可能性があります。「ポップオーバーだけ閉じる」という意図なら、この effect 内で `setShowMore(false)` だけでなく対象ポップオーバー state をまとめて閉じるか、コメントを実装に合わせて具体化してください。


## 影響分析 (Claude read-only)

- **何が変わるか**: スマホでチャンネル一覧→チャンネルへ移動した瞬間、サイドバー（左メニュー）が先に閉じてしまい、白い読み込み画面が一瞬見える違和感を解消します。サイドバーを閉じるタイミングを「チャンネル画面が表示された時」までずらすことで、横スライド遷移が滑らかに見えるようになります。
- **影響範囲**: 変更は bottom-tab-bar.tsx の useEffect 1ヶ所のみ。`setSidebarOpen` 自体はこのファイル内のホーム/進行中/カレンダー/決定タブのタップ処理（73,87,101,115行目）で引き続き使われており、props から外す必要はありません（Codexの1点目は誤指摘）。チャンネル遷移時の閉処理は channel-view.tsx 287行目の `setSidebarOpen(false)` がマウント時に実行するため担保されています（Codexの2点目はOK）。他ページ（DM一覧/設定/検索など）への遷移時の閉処理は各ページ側で `setSidebarOpen(false)` が呼ばれているため回帰しません。
- **LEVEL**: 軽微
- **根拠**: 変更は同コンポーネント内の閉タイミング調整のみで、サイドバー閉処理は遷移先ページ側で既に担保されている。


## Claude Code 修正 (応答)

## 採否

- **指摘1 (未使用変数)**: 不採用。`setSidebarOpen` は同ファイル73/87/101/115行 (ホーム/進行中/カレンダー/決定タブ) で使用中。誤指摘。
- **指摘2 (ChannelView 側の保証)**: 不採用。`channel-view.tsx:287` で `useEffect(() => { setSidebarOpen(false); ... })` 実装済みを確認。
- **指摘3 (コメント具体化)**: 採用。

## 変更内容

`bottom-tab-bar.tsx:27-32` のコメントを具体化し、(1) ここで閉じるのは `showMore` のみ、(2) サイドバーは `ChannelView` 側で閉じる、(3) `showBookmark` / `showMembers` は pathname 同期不要 (モーダルから直接遷移する導線がない) ことを明記。


## 修正後 diff (vs HEAD)

```
diff --git a/src/components/bottom-tab-bar.tsx b/src/components/bottom-tab-bar.tsx
index 502f97f..25f2896 100644
--- a/src/components/bottom-tab-bar.tsx
+++ b/src/components/bottom-tab-bar.tsx
@@ -24,11 +24,12 @@ export function BottomTabBar({ workspaceSlug, workspaceId, currentUserId, member
   const [showBookmark, setShowBookmark] = useState(false);
   const [showMembers, setShowMembers] = useState(false);
 
-  // URL が変わったらポップオーバーとサイドバーを閉じる。
-  // （iOS で "タップしても画面変わらない" と見えていたのは、実は navigation は
-  // 成功していて、開きっぱなしのサイドバーが新しいページの上を覆っていただけ）
-  // ただし初回マウント時は skip: ワークスペーストップ /ws-xxx でサイドバーを
-  // 自動表示する WorkspaceLobby の setSidebarOpen(true) を打ち消してしまうため
+  // URL が変わったら「その他」ポップオーバー (showMore) のみ閉じる。
+  // サイドバーはここでは閉じない: チャンネル遷移時は ChannelView マウント時
+  // (channel-view.tsx の useEffect) で閉じる。pathname 変更直後にここで閉じると
+  // loading だけが先に表示されて白く見えるため。
+  // showBookmark / showMembers はモーダルで、開いた状態から直接 URL 遷移する
+  // 導線がないので pathname 同期は不要。
   const initialMountRef = useRef(true);
   // pathname 変化（外部のナビゲーション state）に同期して UI を閉じる。
   // 派生 state では計算できないため effect 内で setState する正当なケース
@@ -39,8 +40,7 @@ export function BottomTabBar({ workspaceSlug, workspaceId, currentUserId, member
     }
     // eslint-disable-next-line react-hooks/set-state-in-effect
     setShowMore(false);
-    setSidebarOpen(false);
-  }, [pathname, setSidebarOpen]);
+  }, [pathname]);
 
   // ポップオーバー外タップで閉じる（その他ボタン自身と中身は除外）
   useEffect(() => {

```


## ステータス

✅ 修正適用完了
