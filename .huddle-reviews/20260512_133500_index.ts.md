# supabase/functions/send-push/index.ts (2026-05-12T13:35:10)

**種別**: Git diff (vs HEAD)


## 変更前 diff

```
diff --git a/supabase/functions/send-push/index.ts b/supabase/functions/send-push/index.ts
index db4a4e2..5383b33 100644
--- a/supabase/functions/send-push/index.ts
+++ b/supabase/functions/send-push/index.ts
@@ -310,7 +310,9 @@ Deno.serve(async (req) => {
         ? (channel as unknown as { workspaces: { slug: string }[] }).workspaces[0]?.slug
         : (channel as unknown as { workspaces: { slug: string } }).workspaces.slug);
     const channelUrl =
-      workspaceSlug && channel.slug ? `/${workspaceSlug}/${channel.slug}` : "/";
+      workspaceSlug && channel.slug
+        ? `/${workspaceSlug}/${channel.slug}?m=${record.id}`
+        : "/";
 
     // 受信者: チャンネルメンバー全員（送信者除外）— LINE方式
     // スレッド返信・通常メッセージ・投票・決定事項すべて同一ロジック

```


## Codex レビュー

- [優先度: 今すぐ] `record.id` を URL に直埋めしているため、将来 ID 形式が変わった場合や不正値混入時に URL が壊れます。`?m=${encodeURIComponent(String(record.id))}` または `new URLSearchParams({ m: String(record.id) })` で生成してください。

- [優先度: 後で] `channelUrl` 生成のテストを追加してください。少なくとも `workspaceSlug` / `channel.slug` がある場合に `/${workspace}/${channel}?m=${record.id}` になること、欠ける場合は `/` になることを確認すると、この deep link 仕様の退行を防げます。


## 影響分析 (Claude read-only)

- **何が変わるか**: 通知をタップしたとき、これまではチャンネルの先頭に飛ぶだけでしたが、今回の変更で「通知の元になった投稿そのもの」に直接ジャンプできるようになります。
- **影響範囲**: 変更箇所は send-push の通知URL生成ロジックのみで、`channelUrl` はこの関数内 (474行目で通知ペイロードに渡すだけ) で完結しています。grep の結果、`?m=` というクエリは push-tap-handler / channel-view / activity-modal などで既にジャンプキーとして使われており、受信側は変更不要でそのまま動きます。同種の send-reaction-push / send-vote-push にも同じ書き換えが入っているので一貫性は保たれます。
- **LEVEL**: 軽微
- **根拠**: channelUrl は同関数スコープのローカル変数で外部参照なし、`?m=` 受け側は既存実装に対応済み。


## ステータス

ユーザーが『スキップ』選択
