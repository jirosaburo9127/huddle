# src/app/(workspace)/[workspace]/[channel]/components/message-item.tsx (2026-05-11T22:26:13)

**種別**: Git diff (vs HEAD)


## 変更前 diff

```
diff --git a/src/app/(workspace)/[workspace]/[channel]/components/message-item.tsx b/src/app/(workspace)/[workspace]/[channel]/components/message-item.tsx
index c0c91d4..25fef36 100644
--- a/src/app/(workspace)/[workspace]/[channel]/components/message-item.tsx
+++ b/src/app/(workspace)/[workspace]/[channel]/components/message-item.tsx
@@ -417,7 +417,7 @@ function ReactionBadges({
                       {EMOJI_LIST.map((group) => (
                         <div key={group.category} className="mb-3">
                           <p className="text-[11px] text-muted font-medium mb-1.5">{group.category}</p>
-                          {group.category === "テキスト" ? (
+                          {group.kind === "text" ? (
                             <div className="flex flex-wrap gap-1.5">
                               {group.emojis.map((emoji) => (
                                 <button
@@ -1267,7 +1267,7 @@ export const MessageItem = memo(function MessageItem({
               {EMOJI_LIST.map((group) => (
                 <div key={group.category} className="mb-3">
                   <p className="text-[11px] text-muted font-medium mb-1.5">{group.category}</p>
-                  {group.category === "テキスト" ? (
+                  {group.kind === "text" ? (
                     <div className="flex flex-wrap gap-1.5">
                       {group.emojis.map((emoji) => (
                         <button

```


## Codex レビュー

LGTM


## ステータス

修正適用スキップ: Codex が approve
