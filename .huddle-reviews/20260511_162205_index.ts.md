# supabase/functions/mikan-respond/index.ts (2026-05-11T16:22:05)

**種別**: Git diff (vs HEAD)  **ターン数**: 3


## 変更内容

```
diff --git a/supabase/functions/mikan-respond/index.ts b/supabase/functions/mikan-respond/index.ts
index 4332c0e..30829cb 100644
--- a/supabase/functions/mikan-respond/index.ts
+++ b/supabase/functions/mikan-respond/index.ts
@@ -1,13 +1,21 @@
 // Supabase Edge Function: みかん（AIファシリテーター）の返信生成
 //
-// 単一モード (mention): mentions テーブルへの INSERT が起点
-//   - @みかん で呼ばれた時。会話 + ツール (propose_event 等) を渡し、
-//     テキスト返信もしくは予定提案を投稿する。
-//   - 予定登録の判断は LLM のプロンプト側で完結させる
-//     (場所未確認・過去日・日時不明なら確認質問を返す)。
+// 動作モード: mention のみ (ユーザー起点)
+//   - mentions テーブルへの INSERT が起点。@みかん で呼ばれた時に
+//     会話 + 4 つのツール (propose_event / set_event_reminder / web_search /
+//     web_fetch) を渡し、テキスト返信もしくは予定提案を投稿する。
+//   - 予定登録の一次判断 (場所未確認・過去日・日時不明なら確認質問を返す)
+//     は LLM のシステムプロンプトで誘導する想定。コード側にも保険フィルタは
+//     置かない。LLM が誤判定したらユーザーがその場で訂正できる前提。
+//   - したがって「期待した振る舞い」であり、強い実装保証ではない点に注意。
 //
-// なお messages テーブルへの INSERT (旧 listen モード = 自動見守り提案)
-// は廃止した。ユーザー起点 (Mode A) のみで運用する。
+// 旧 messages テーブル INSERT 起点 (自動見守り = 旧 Mode B) は廃止済み。
+//   - 理由: 自動モードは「保険フィルタ禁止」方針と相性が悪く、重複防止や
+//     場所未確認 skip などのコード側バリデーションを後付けで積む方向に
+//     走ってしまうため。ユーザー起点の Mode A 1 本に絞った。
+//   - DB 側のトリガー (messages_mikan_listen_trigger / notify_mikan_listen)
+//     は migration 117 で DROP 済み。Edge Function 側でも messages payload は
+//     早期 return で受け付けない (Webhook が残っていても no-op)。
 //
 // 環境変数 (Supabase Secrets):
 //   ANTHROPIC_API_KEY        : Anthropic Console で発行する API キー

```


## Claude #1

レビューします。今回の差分はヘッダコメントのみの変更で、実行コードに変化なし、という前提でのレビューです。

## バグ/エッジケース漏れ
- **記載内容と実装の整合性が未検証** (任意): 「Edge Function 側でも messages payload は早期 return で受け付けない」と書いてあるが、実装でその early return が本当に存在するか diff からは分からない。コメントが実装より先行している/取り残されている事故が起きやすい箇所なので、該当行へのアンカー (例: `// see L<NNN>`) があると安全。
- **migration 117 への固有名参照** (後で): `messages_mikan_listen_trigger` / `notify_mikan_listen` / `migration 117` という固有名がコメントに焼き込まれている。改名・採番ズレで嘘ドキュメント化するリスクあり。「過去に DROP 済み (詳細は migrations 履歴参照)」程度の抽象度の方が腐りにくい。

## 設計/可読性
- **「強い実装保証ではない」宣言は◎** (今すぐ採用): LLM の一次判断に委ねていること、保険フィルタを置かない方針、を明文化したのは将来「重複防止を足そう」誘惑への防波堤として価値が高い。M3 (DHH) / M4 (Jobs) 観点でも過剰設計の予防になる。
- **「Mode A」「旧 Mode B」用語の初出**: 旧コメントには無かった用語。コードベース全体で同じ呼称が使われているなら良いが、ここだけのローカル用語なら脚注かリンクが欲しい。
- **冗長化のトレードオフ**: 行数が約 2 倍。背景説明として有用だが、ヘッダはスキャナビリティ優先な場所。3 行サマリ + 「### 詳細」セクション分けの方が読者に親切かも (任意)。

## セキュリティ/入力検証
- **`web_search` / `web_fetch` ツールの存在が明示された** (後で): ヘッダで露出させた以上、別箇所に SSRF / 外部宛先制限 / レート制御の方針も書いておきたい。今は「ツールを 4 つ渡す」事実だけが書かれ、安全側の前提が不明 (M11 シュナイアー観点)。
- **「LLM 誤判定はユーザー訂正前提」は脅威モデルとしては弱い**: 場所未確認・過去日・誤った予定登録が **副作用を伴う** (例: カレンダー API への書き込み、リマインダー発火) 場合、ユーザー訂正前に外部影響が出る。コメントで「副作用ツールは確認ステップ経由」と一言補強する余地あり。

## 優先度
- **今すぐ**: なし (コメントのみで実害なし)
- **後で**:
  - migration 番号/トリガー名の固有参照を抽象化
  - `web_search` / `web_fetch` のセキュリティ前提を 1〜2 行追記
- **任意**:
  - 早期 return 実装箇所への行アンカー
  - ヘッダのサマリ/詳細分割

## Codex への申し送り
論点として残したいのは 2 点:
1. 「保険フィルタを置かない」方針を**コメント以外の場所** (テスト、CI、PR テンプレ等) でも担保すべきか
2. `web_fetch` 系ツールのサンドボックス境界はどこで定義されているか (このファイル外を見る必要あり)


## Codex #2

実体ファイルはこの workspace では見つからなかったため、提示 diff ベースのセカンドオピニオンです。

**同意**
- 「コメントのみなので今すぐブロッカーなし」は妥当。
- 「LLM 判断であり実装保証ではない」と明記した点は有益。将来の誤解を減らす。
- `messages payload は早期 return` は、実装とズレると危険なので確認対象にする価値あり。

**見落とし気味**
- 「コード側にも保険フィルタは置かない」はかなり強い方針なので、コメントだけだと将来変更時に破られやすい。テストよりも、まずは ADR/設計メモ/PR 説明など “方針の置き場所” があるかを見るのがよさそう。
- `web_search / web_fetch` は、このヘッダコメントでセキュリティ設計まで背負わせるより、ツール定義・実行箇所に制約があるかを確認する方が本筋。

**過剰指摘かも**
- `migration 117` やトリガー名の固有参照は、むしろ追跡性が上がるので一概に悪くない。頻繁にリベースや採番変更が起きる運用でなければ残してよい。
- 行アンカー `see L<NNN>` は壊れやすいので不要寄り。関数名や分岐条件名への参照の方がまだ保守しやすい。
- ヘッダの「3 行サマリ + 詳細」分割は好みの範囲。今回のコメントは長いが、運用方針の明文化としては許容。


## Claude #3

## Codex への返答

**同意**
- `migration 117` 固有名参照の件、撤回します。Codex の「追跡性向上」観点が妥当。リベース頻度が高くない運用なら残す方が grep 起点になって有益。私の「腐りやすさ」懸念は過剰でした。
- 行アンカー `L<NNN>` も同意で取り下げ。行番号は最も壊れやすいので、むしろ関数名 (例: `handleMessagesPayload` のような) で参照する方が良い。
- `web_search`/`web_fetch` のセキュリティ前提を**このヘッダに書け**という指摘は確かに筋が悪い。ツール定義側 (おそらく同ファイル内のツール宣言箇所、または共通ラッパー) に書くのが本筋。

**新規論点**
- 「保険フィルタを置かない」方針の置き場所について: ADR/設計メモが望ましいというのは同意だが、このリポジトリに ADR 運用があるかは未確認。**無いなら CLAUDE.md か README に 1 行 "Mode A only / no defensive filters" を昇格させる**のが現実解では。ヘッダコメントは「ファイルを開いた人」しか読まないので、方針としては露出が弱い。

**残る確認事項 (Codex への申し送り継続)**
- messages payload の早期 return が実装に存在するか → 実体未確認。これは diff ではなくファイル全文を見ないと判定不能なので、レビュアー側で実装確認すべき項目として PR コメントに残すのが妥当。

優先度: 「方針の置き場所」(CLAUDE.md or ADR 昇格) のみ実アクション、他は許容。
