# src/app/(workspace)/[workspace]/[channel]/components/channel-view.tsx (2026-05-12T18:06:31)

**種別**: Git diff (vs HEAD)


## 変更前 diff

```
diff --git a/src/app/(workspace)/[workspace]/[channel]/components/channel-view.tsx b/src/app/(workspace)/[workspace]/[channel]/components/channel-view.tsx
index 7d39782..ecff74a 100644
--- a/src/app/(workspace)/[workspace]/[channel]/components/channel-view.tsx
+++ b/src/app/(workspace)/[workspace]/[channel]/components/channel-view.tsx
@@ -895,6 +895,8 @@ export function ChannelView({ channel, initialMessages, currentUserId, initialLa
             sentReactionIdsRef.current.delete(newReaction.id);
             return;
           }
+          // 表示中メッセージに関係ないリアクションは処理しない
+          if (!messagesRef.current.some((m) => m.id === newReaction.message_id)) return;
           setMessages((prev) =>
             prev.map((m) => {
               if (m.id !== newReaction.message_id) return m;

```


## Codex レビュー

- [優先度: 今すぐ] `messagesRef.current.some(...)` の事前ガードは、同じリアルタイムバッチ内で「メッセージ追加 → リアクション追加」が連続した場合に stale な `messagesRef` を見てリアクションを捨てる可能性があります。`setMessages((prev) => { const exists = prev.some(...); if (!exists) return prev; return prev.map(...); })` のように functional updater 内で判定してください。

- [優先度: 後で] `some` と `map` で同じ配列を2回走査しています。上記修正時に `found` フラグ付きの1回の `map`、または updater 内の `some` 後に必要時だけ `map` にして、不要な `setMessages` を避ける形にまとめるとよいです。

- [優先度: 後で] 「表示済みメッセージ直後にリアクションイベントが届く」ケースの回帰テストを追加してください。特に、メッセージ追加の `setMessages` が先に enqueue され、`messagesRef` 更新前にリアクション処理が走るケースを再現できると安全です。


## 影響分析 (Claude read-only)

- **何が変わるか**: 他人の絵文字リアクションがリアルタイムで届いたとき、いま画面に表示していないメッセージへのリアクションは処理を省略して、画面更新の無駄な再描画を減らします。
- **影響範囲**: 変更箇所は ChannelView コンポーネント内のリアルタイム購読ハンドラだけです。grep で messagesRef / sentReactionIdsRef を検索しても、定義・参照ともこの channel-view.tsx 1ファイルに閉じており、他ページ(DM、サイドバー、ワークスペース設定など)からは触られていません。ChannelView 自体は page.tsx と sidebar.tsx から参照されていますが、今回の差分は内部実装の早期 return 追加だけで、外部に露出した動作は変わりません。
- **LEVEL**: 軽微
- **根拠**: 変更は ChannelView 内クロージャの1行ガードのみで、外部から参照される API や状態は変わっていない。


## ステータス

ダイアログタイムアウト → スキップ
