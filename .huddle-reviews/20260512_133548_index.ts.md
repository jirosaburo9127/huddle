# supabase/functions/send-reaction-push/index.ts (2026-05-12T13:36:04)

**種別**: Git diff (vs HEAD)


## 変更前 diff

```
diff --git a/supabase/functions/send-reaction-push/index.ts b/supabase/functions/send-reaction-push/index.ts
index f8dd0b6..3e3e388 100644
--- a/supabase/functions/send-reaction-push/index.ts
+++ b/supabase/functions/send-reaction-push/index.ts
@@ -125,7 +125,9 @@ Deno.serve(async (req) => {
         ? (channel as unknown as { workspaces: { slug: string }[] }).workspaces[0]?.slug
         : (channel as unknown as { workspaces: { slug: string } }).workspaces.slug);
     const channelUrl =
-      workspaceSlug && channel.slug ? `/${workspaceSlug}/${channel.slug}` : "/";
+      workspaceSlug && channel.slug
+        ? `/${workspaceSlug}/${channel.slug}?m=${record.message_id}`
+        : "/";
 
     // 通知先: メッセージ投稿者のみ
     const recipientId = message.user_id;

```


## Codex レビュー

- [優先度: 今すぐ] `record.message_id` を URL に直接埋め込んでいます。UUID想定でも、URL生成箇所では `encodeURIComponent(record.message_id)` か `URLSearchParams` を使ってください。例: ``?m=${encodeURIComponent(record.message_id)}``。

- [優先度: 後で] `channelUrl` の生成テストを追加してください。期待値として `/{workspaceSlug}/{channelSlug}?m={message_id}` になるケースと、`workspaceSlug` / `channel.slug` が欠けた場合に `/` になるケースを押さえると、この通知リンクの回帰を防げます。


## 影響分析 (Claude read-only)

- **何が変わるか**: 自分のメッセージにリアクションが付いた通知をタップした時に、そのリアクションされた本文まで自動でジャンプするようになります。今までは該当チャンネルを開くだけで、どのメッセージか分かりませんでした。
- **影響範囲**: 変更は send-reaction-push 関数の中だけで、`channelUrl` も同ファイル内（127行目で生成し189行目でPush通知のurlとして送出）でのみ使われています。grepした結果、他ファイルからの参照はありません。同じ仕組みは送信通知用 send-push と投票通知用 send-vote-push にも既に入っており、リアクション通知だけ未対応だったのを揃える形です。
- **LEVEL**: 影響なし
- **根拠**: `channelUrl` は同関数内クロージャで、外部から参照されていない。


## ステータス

ユーザーが『スキップ』選択
