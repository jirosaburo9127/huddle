# src/components/push-tap-handler.tsx (2026-05-12T13:37:49)

**種別**: Git diff (vs HEAD)


## 変更前 diff

```
diff --git a/src/components/push-tap-handler.tsx b/src/components/push-tap-handler.tsx
index ad5785e..1a92ecc 100644
--- a/src/components/push-tap-handler.tsx
+++ b/src/components/push-tap-handler.tsx
@@ -42,10 +42,14 @@ export function PushTapHandler() {
             }
 
             // 2) フォールバック: 1.2 秒経っても遷移していなければハードナビ
+            //    pathname だけでなく search (クエリ文字列) も含めて比較する
+            //    （?m=<id> 付き URL で同一チャンネルだと pathname が同じでも遷移が必要）
             setTimeout(() => {
               try {
-                const expected = new URL(url, window.location.origin).pathname;
-                if (window.location.pathname !== expected) {
+                const parsed = new URL(url, window.location.origin);
+                const current = window.location.pathname + window.location.search;
+                const expected = parsed.pathname + parsed.search;
+                if (current !== expected) {
                   window.location.href = url;
                 }
               } catch {

```


## Codex レビュー

- [優先度: 後で] `src/components/push-tap-handler.tsx` のフォールバック比較に `hash` が含まれていません。通知URLが同一 `pathname + search` で `#...` だけ異なる場合、遷移済み扱いになり `window.location.href` が発火しません。必要なら `parsed.pathname + parsed.search + parsed.hash` / `window.location.pathname + window.location.search + window.location.hash` で比較してください。

- [優先度: 後で] この変更を守るテストが欲しいです。同一チャンネルで `pathname` は同じだが `?m=<id>` が異なるケースで、1.2秒後に `window.location.href` が呼ばれることを追加してください。


## 影響分析 (Claude read-only)

- **何が変わるか**: プッシュ通知をタップしても同じチャンネル内の特定メッセージへ正しく飛ばないことがある不具合を直します。これまでは URL のチャンネル部分だけを見て「もう移動した」と判定していたため、同じチャンネル内の別メッセージ (?m=xxx) を開きたい通知が無視されることがありました。今回の修正で「?m=xxx」までを含めて判定するので、同一チャンネル内のメッセージ通知でも確実にジャンプできるようになります。
- **影響範囲**: 変更はネイティブアプリ（iOS/Android）でプッシュ通知をタップしたときの動きだけに限定されます。grep で `PushTapHandler` を検索しましたが、参照しているのはアプリ全体のレイアウト (`src/app/layout.tsx`) でマウントしている1箇所のみで、PC/Web 側の挙動や他のページ・コンポーネントには影響しません。比較対象に `hash` (#...) が含まれていない点と、テストが追加されていない点はレビュー指摘どおり残課題ですが、現状の通知 URL では実害は出ていません。
- **LEVEL**: 軽微
- **根拠**: 変更は push-tap-handler.tsx 内のフォールバック判定のみで、外部 export されているのは唯一のマウント箇所だけ。


## ステータス

ユーザーが『スキップ』選択
