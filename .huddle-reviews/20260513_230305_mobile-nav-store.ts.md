# src/stores/mobile-nav-store.ts (2026-05-13T23:03:09)

**種別**: Git diff (vs HEAD)


## 変更前 diff

```
diff --git a/src/stores/mobile-nav-store.ts b/src/stores/mobile-nav-store.ts
index 16ffa19..f17df50 100644
--- a/src/stores/mobile-nav-store.ts
+++ b/src/stores/mobile-nav-store.ts
@@ -5,6 +5,10 @@ type MobileNavStore = {
   setSidebarOpen: (open: boolean) => void;
   pendingDetailOpen: boolean;
   setPendingDetailOpen: (pending: boolean) => void;
+  detailTransitionTitle: string | null;
+  startDetailOpen: (title?: string) => void;
+  detailEnterVersion: number;
+  triggerDetailEnter: () => void;
   // メッセージ入力欄にフォーカスがある時 true。
   // ボトムタブバーがキーボードと一緒に画面を狭めないよう非表示化するために使う。
   messageInputFocused: boolean;
@@ -16,7 +20,14 @@ export const useMobileNavStore = create<MobileNavStore>((set) => ({
   sidebarOpen: true,
   setSidebarOpen: (open) => set({ sidebarOpen: open }),
   pendingDetailOpen: false,
-  setPendingDetailOpen: (pending) => set({ pendingDetailOpen: pending }),
+  setPendingDetailOpen: (pending) => set((state) => ({
+    pendingDetailOpen: pending,
+    detailTransitionTitle: pending ? state.detailTransitionTitle : null,
+  })),
+  detailTransitionTitle: null,
+  startDetailOpen: (title) => set({ pendingDetailOpen: true, detailTransitionTitle: title ?? null }),
+  detailEnterVersion: 0,
+  triggerDetailEnter: () => set((state) => ({ detailEnterVersion: state.detailEnterVersion + 1 })),
   messageInputFocused: false,
   setMessageInputFocused: (focused) => set({ messageInputFocused: focused }),
 }));

```


## Codex レビュー

- [優先度: 今すぐ] `setPendingDetailOpen(true)` が既存の `detailTransitionTitle` を保持するため、前回のタイトルが残る可能性があります。`setPendingDetailOpen` は単純に `detailTransitionTitle: null` へリセットし、タイトル付き開始は `startDetailOpen(title)` に統一してください。

- [優先度: 後で] `startDetailOpen` と `triggerDetailEnter` が分かれているため、呼び出し側が片方を忘れると状態遷移とアニメーション契機がずれます。詳細オープン開始時に必ず enter を発火する設計なら、`startDetailOpen` 内で `detailEnterVersion` も increment する形にまとめると安全です。


## 影響分析 (Claude read-only)

- **何が変わるか**: モバイルで一覧から詳細画面へ切り替わる時のスライドアニメーションを安定させる仕組みを追加します。詳細を開く合図に合わせて切替中に表示するタイトルを保持できるようにし、何度同じ詳細を開いてもアニメーションがやり直し再生される仕組みを用意します。
- **影響範囲**: 共有ストアの定義変更なので複数ファイルに波及します。grepの結果、`setPendingDetailOpen` は sidebar.tsx (3箇所) と channel-view.tsx (1箇所) で呼ばれており、今回の変更で「true にしても既存のタイトルが残らずクリアされる」挙動に変わります。新規追加の `detailEnterVersion` は main-pane.tsx で監視され、`triggerDetailEnter` は channel-view.tsx から呼ばれ、`detailTransitionTitle` は mobile-detail-transition.tsx で参照されます。
- **LEVEL**: 注意
- **根拠**: 別ファイル4つ (sidebar / main-pane / channel-view / mobile-detail-transition) から参照される共有ストアの公開APIを変更しているため。


## ステータス

ユーザーが『スキップ』選択
