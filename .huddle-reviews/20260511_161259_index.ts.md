# supabase/functions/mikan-respond/index.ts (2026-05-11T16:12:59)

**種別**: Git diff (vs HEAD)  **ターン数**: 3


## 変更内容

```
diff --git a/supabase/functions/mikan-respond/index.ts b/supabase/functions/mikan-respond/index.ts
index 086bdcc..0f3843d 100644
--- a/supabase/functions/mikan-respond/index.ts
+++ b/supabase/functions/mikan-respond/index.ts
@@ -1,14 +1,13 @@
 // Supabase Edge Function: みかん（AIファシリテーター）の返信生成
 //
-// 2モード:
-//  Mode A (mention): mentions テーブルへの INSERT が起点
-//    - @みかん で呼ばれた時。会話 + ツール (propose_event) を渡し、
-//      テキスト返信もしくは予定提案を投稿する。
-//  Mode B (listen): messages テーブルへの INSERT が起点
-//    - mikan-enabled チャンネルで誰かが投稿するたび呼ばれる。
-//    - チャンネル内に最近 30 分以内の提案があればクールダウン。
-//    - propose_event ツールのみ提供。テキスト返信は投稿しない (見守るだけ)。
-//    - 直近の文脈で日時が確定したと判断した時のみ提案する。
+// 単一モード (mention): mentions テーブルへの INSERT が起点
+//   - @みかん で呼ばれた時。会話 + ツール (propose_event 等) を渡し、
+//     テキスト返信もしくは予定提案を投稿する。
+//   - 予定登録の判断は LLM のプロンプト側で完結させる
+//     (場所未確認・過去日・日時不明なら確認質問を返す)。
+//
+// なお messages テーブルへの INSERT (旧 listen モード = 自動見守り提案)
+// は廃止した。ユーザー起点 (Mode A) のみで運用する。
 //
 // 環境変数 (Supabase Secrets):
 //   ANTHROPIC_API_KEY        : Anthropic Console で発行する API キー

```


## Claude #1

# コードレビュー

提示された diff は **ヘッダーコメントのみ** の変更で、実コードの差分は含まれていません。前提として「コメントと実装が一致しているか」が確認できないため、以下は **コメント単体の評価＋実装側で要確認の事項** として整理します。

## バグ / エッジケース漏れ
- **【今すぐ】Mode B 実装の残骸チェック**: コメントから listen モードを削除しただけで、`messages` テーブルの webhook ハンドラ・クールダウン判定 (30分) ・分岐ロジックが本体に残っていないか要確認。残っていればデッドコード or 想定外起動のリスク。
- **【今すぐ】Database Webhook 側の整合性**: Supabase ダッシュボードの `messages` INSERT トリガー webhook を **無効化 or 削除** したか? 関数側だけ廃止して webhook が残ると、毎回 invoke されて課金・ログが膨らむ + 404/no-op で失敗ログが出続ける。
- **【後で】"propose_event 等"**: 旧コメントは `propose_event` のみだったが、新コメントは「等」と複数ツールを示唆。実装でツールが増えたなら名称を列挙するか、増えていないなら「等」を外す。

## 設計 / 可読性
- **【任意】コメント自体は良好**: 「廃止した。ユーザー起点のみで運用」と歴史的経緯を残しているのは将来の読み手に親切 (なぜ Mode B が無いのか問われない)。
- **【後で】廃止理由の一行**: 「自動提案がノイジーだった」「LLM 判定が不安定だった」など廃止の **Why** を 1 行入れると、半年後に「再導入したい」と言われたとき判断材料になる。
- **【任意】`Mode A` 表記の整理**: 「単一モード (mention)」と言いつつ末尾で「Mode A のみで運用」と Mode A 表記が残る。単一モードなら Mode A という呼称ごと消すか、明示的に残すか統一を。

## セキュリティ / 入力検証
- **diff 範囲では影響なし** (コメントのみ)。
- **【今すぐ・実装側】** listen モード廃止に伴い、`messages` 由来の入力 (チャンネル全投稿) を信頼境界として扱っていた箇所がなくなったので攻撃面は **縮小**。逆に `mentions` 経由のみが入口になるので、mention payload の検証 (channel_id/user_id 改ざん耐性、署名検証) を再確認推奨。

## 優先度サマリ
| 優先度 | 項目 |
|--------|------|
| 今すぐ | Mode B 関連コードの残骸削除、Database Webhook の停止確認 |
| 今すぐ | mention webhook の入力検証再確認 |
| 後で | "propose_event 等" の整合、廃止理由 1 行追加 |
| 任意 | Mode A 呼称の統一 |

**Codex への論点**: 「コメントを単一モード化したが、実コード/インフラ (webhook 設定) が本当にシングルモードになっているか」が最大の論点です。実コード差分を提示してもらえると Mode B 撤去の完全性を判定できます。


## Codex #2

同意点:
- Claude の最大論点「コメント変更だけでは実装・Webhook 設定との整合が確認できない」は妥当です。
- `"propose_event 等"` は軽いリスクあり。現状ツールが `propose_event` だけなら「等」は外した方が正確です。
- `単一モード` と `Mode A` の併記はやや不統一。コメント品質の範囲ですが直す価値はあります。

見落とし:
- 新コメントの「場所未確認・過去日・日時不明なら確認質問を返す」は、単なる運用説明ではなく **LLM の期待動作仕様** になっています。実プロンプトやテストで担保されていないなら、コメントが実装保証を過剰に見せる可能性があります。
- 「予定登録の判断は LLM のプロンプト側で完結」は少し危うい表現です。過去日・日時不明などは可能ならコード側のバリデーションでも守るべきで、LLM 任せに読めます。

過剰指摘:
- mention payload の署名検証・改ざん耐性は、この diff がコメントのみならやや射程外です。レビュー観点としては悪くないですが、「今すぐ」級にするには実装差分や既存脆弱性の根拠が欲しいです。
- 廃止理由の追記は任意で十分。ヘッダーコメントが長くなりすぎるなら不要です。

新規で一番気になるのは、コメントが「実装の現状説明」から「LLM が必ずこう振る舞うという仕様宣言」に寄っている点です。担保が薄いなら表現を弱めるか、プロンプト・テスト側で保証した方がよいです。


## Claude #3

## Codex への返答

**同意（重要）**:
- **「LLM プロンプト側で完結」表現の危うさ**: その通りです。コメントが仕様宣言化している点を見落としていました。過去日・場所未確認は **コード側のガード (Zod 等で date >= now、必須フィールド検証)** を入れた上で、コメントは「一次判断は LLM、最終バリデーションはコード」と書く方が安全。LLM 任せはプロンプトインジェクションや幻覚で容易に崩れます。
- **コメントが実装保証を過剰に見せる**: 同意。テスト・プロンプトで担保がないなら、断定形 (「〜なら確認質問を返す」) ではなく「〜することを期待する」程度に弱める、もしくは該当プロンプトファイルへのリンクを併記すべき。

**同意（軽め）**:
- 廃止理由追記が任意なのは同意。Why が必要なら git log / PR 説明で足りる。
- mention 署名検証の「今すぐ」格上げは確かに勇み足でした。実装差分が無いので **「実装確認時のチェックリスト」レベル** に格下げします。

**新規論点 (1点だけ)**:
- 「listen モード廃止」は **動作変更を伴うはず** なのに diff がコメントのみなのが不自然。実コード側で `if (table === 'messages') return` のような早期 return が既に入っているのか、それとも別 PR / 別コミットで実装変更済みなのかを Author に確認したい。**コメントだけ先に直して実装が後追い**だと、デプロイタイミングで挙動とコメントが食い違う期間が生じます。
