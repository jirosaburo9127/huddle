# src/components/sidebar.tsx (2026-05-12T13:50:16)

**種別**: Git diff (vs HEAD)


## 変更前 diff

```
diff --git a/src/components/sidebar.tsx b/src/components/sidebar.tsx
index 597db1e..10860e9 100644
--- a/src/components/sidebar.tsx
+++ b/src/components/sidebar.tsx
@@ -889,7 +889,7 @@ export function Sidebar({
               {searchResults.map((r) => (
                 <Link
                   key={r.id}
-                  href={`/${workspaceSlug}/${r.channel_slug}`}
+                  href={`/${workspaceSlug}/${r.channel_slug}?m=${r.id}`}
                   onClick={() => { setSearchQuery(""); setSearchResults([]); }}
                   className="block px-3 py-2.5 border-b border-border/30 hover:bg-white/[0.04] transition-colors"
                 >

```


## Codex レビュー

- [優先度: 今すぐ] `r.id` を URL クエリに直入れしているため、ID に `&` や `#` などが混ざると壊れます。`href={`/${workspaceSlug}/${r.channel_slug}?m=${encodeURIComponent(r.id)}`}` にしてください。

- [優先度: 後で] `workspaceSlug` / `channel_slug` もパスセグメントとして直結されています。既存実装の延長ですが、検索結果経由でリンク先を組み立てるなら `encodeURIComponent(workspaceSlug)` と `encodeURIComponent(r.channel_slug)` を使う形に寄せると、特殊文字入り slug での遷移不具合を避けられます。

- [優先度: 後で] この変更は「検索結果クリックで対象メッセージへ移動する」挙動なので、`?m=<messageId>` を受け取る側の動作テストが必要です。検索結果クリック後に正しいチャンネルへ遷移し、対象メッセージが選択・スクロール・ハイライトされることをテストに追加してください。


## 影響分析 (Claude read-only)

- **何が変わるか**: サイドバーの検索結果をクリックしたとき、これまでは「対象のチャンネルを開くだけ」でしたが、これからは「対象のチャンネルを開いて、その投稿まで自動的にジャンプ・ハイライトする」ようになります。
- **影響範囲**: 変更は sidebar.tsx の検索結果リンク1行のみで、関数の定義やexportは増減していません。受け取り側の `?m=<投稿ID>` 処理は channel-view.tsx に既存実装があり（grepで7箇所ヒット：l.42, l.328, l.400, l.448 など。進行中まとめ・新規投稿シェアなど他経路でも同じURL形式を既に使用中）、今回はそこに新しい入口を1つ追加した形になります。SearchResult型は sidebar.tsx 内のローカル定義で外部参照なし。
- **LEVEL**: 軽微
- **根拠**: 変更箇所は sidebar.tsx の検索結果Link 1行のみで、`?m=` 受け取り側は既存の共通仕組みを再利用しているため。


## Claude Code 修正 (応答)

採用: `?m=` のクエリ値を `encodeURIComponent(r.id)` でエンコード。

不採用:
- slug の encodeURIComponent化: 既存全体の慣習（他箇所もパス直結）から外れるので、本diffスコープ外として見送り。一括対応が筋。
- テスト追加: 本タスクのスコープ外（指摘どおりだが別タスク）。

変更要約: 検索結果リンクの `?m=${r.id}` を `encodeURIComponent` で包み、ID に特殊文字が混ざってもクエリが壊れないようにした。


## 修正後 diff (vs HEAD)

```
diff --git a/src/components/sidebar.tsx b/src/components/sidebar.tsx
index 10860e9..c92e0e3 100644
--- a/src/components/sidebar.tsx
+++ b/src/components/sidebar.tsx
@@ -889,7 +889,7 @@ export function Sidebar({
               {searchResults.map((r) => (
                 <Link
                   key={r.id}
-                  href={`/${workspaceSlug}/${r.channel_slug}?m=${r.id}`}
+                  href={`/${workspaceSlug}/${r.channel_slug}?m=${encodeURIComponent(r.id)}`}
                   onClick={() => { setSearchQuery(""); setSearchResults([]); }}
                   className="block px-3 py-2.5 border-b border-border/30 hover:bg-white/[0.04] transition-colors"
                 >

```


## ステータス

✅ 修正適用完了
