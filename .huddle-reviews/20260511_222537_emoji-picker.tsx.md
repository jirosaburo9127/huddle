# src/app/(workspace)/[workspace]/[channel]/components/emoji-picker.tsx (2026-05-11T22:26:06)

**種別**: Git diff (vs HEAD)


## 変更前 diff

```
diff --git a/src/app/(workspace)/[workspace]/[channel]/components/emoji-picker.tsx b/src/app/(workspace)/[workspace]/[channel]/components/emoji-picker.tsx
index c37d35e..37fd260 100644
--- a/src/app/(workspace)/[workspace]/[channel]/components/emoji-picker.tsx
+++ b/src/app/(workspace)/[workspace]/[channel]/components/emoji-picker.tsx
@@ -10,11 +10,16 @@ type Props = {
 
 // よく使う絵文字カテゴリ（外部ライブラリ不要）
 // 他所から再利用できるよう export しておく
-export const EMOJI_LIST = [
-  { category: "よく使う", emojis: ["👍", "❤️", "😂", "🎉", "🔥", "👀", "💯", "✅", "🚀"] },
-  { category: "表情", emojis: ["😊", "😄", "🤔", "😮", "😢", "😡", "🥳", "😎"] },
-  { category: "ジェスチャー", emojis: ["👏", "🙌", "🤝", "💪", "✌️", "🫡", "👋", "🙏"] },
-  { category: "テキスト", emojis: ["完了しました！", "了解しました！", "確認中です", "対応いたします", "ありがとうございます！", "お疲れ様です！", "禿同", "かしこ", "m9", "それな", "あざます", "orz"] },
+// kind: "text" は短文定型文（ボタン表示）、"emoji" は通常の絵文字グリッド表示
+export type EmojiGroup =
+  | { kind: "emoji"; category: string; emojis: string[] }
+  | { kind: "text"; category: string; emojis: string[] };
+
+export const EMOJI_LIST: EmojiGroup[] = [
+  { kind: "emoji", category: "よく使う", emojis: ["👍", "❤️", "😂", "🎉", "🔥", "👀", "💯", "✅", "🚀"] },
+  { kind: "emoji", category: "表情", emojis: ["😊", "😄", "🤔", "😮", "😢", "😡", "🥳", "😎"] },
+  { kind: "emoji", category: "ジェスチャー", emojis: ["👏", "🙌", "🤝", "💪", "✌️", "🫡", "👋", "🙏"] },
+  { kind: "text", category: "テキスト", emojis: ["完了しました！", "了解しました！", "確認中です", "対応いたします", "ありがとうございます！", "お疲れ様です！", "禿同", "かしこ", "m9", "それな", "あざます", "orz"] },
 ];
 
 export function EmojiPicker({ onSelect, onClose, position = "below" }: Props) {
@@ -70,7 +75,7 @@ export function EmojiPicker({ onSelect, onClose, position = "below" }: Props) {
           >
             {group.category}
           </p>
-          {group.category === "テキスト" ? (
+          {group.kind === "text" ? (
             <div className="flex flex-wrap gap-1">
               {group.emojis.map((emoji) => (
                 <button

```


## Codex レビュー

- [優先度: 任意] `EmojiGroup` の union は両分岐で `category` / `emojis` が同一なので冗長です。`type EmojiGroup = { kind: "emoji" | "text"; category: string; emojis: string[] }` にすると読みやすくなります。

- [優先度: 任意] `kind: "text"` でもフィールド名が `emojis` のままなので、外部再利用時に誤解しやすいです。将来的に触るなら `items` などにリネームし、参照箇所も合わせて更新すると意図が明確です。


## Claude Code 修正 (応答)

採用: 指摘1（union 簡素化）。`EmojiGroup` を単一型に整理。

却下: 指摘2（`emojis` → `items` リネーム）。任意レベルかつ参照箇所が複数（message-item など）に波及するため、本タスクのスコープ外。

変更内容: `EmojiGroup` を冗長な union から `{ kind: "emoji" | "text"; ... }` の単一型へ。`EMOJI_LIST` の各エントリと利用箇所は無変更（互換）。


## 修正後 diff (vs HEAD)

```
diff --git a/src/app/(workspace)/[workspace]/[channel]/components/emoji-picker.tsx b/src/app/(workspace)/[workspace]/[channel]/components/emoji-picker.tsx
index c37d35e..4452def 100644
--- a/src/app/(workspace)/[workspace]/[channel]/components/emoji-picker.tsx
+++ b/src/app/(workspace)/[workspace]/[channel]/components/emoji-picker.tsx
@@ -10,11 +10,14 @@ type Props = {
 
 // よく使う絵文字カテゴリ（外部ライブラリ不要）
 // 他所から再利用できるよう export しておく
-export const EMOJI_LIST = [
-  { category: "よく使う", emojis: ["👍", "❤️", "😂", "🎉", "🔥", "👀", "💯", "✅", "🚀"] },
-  { category: "表情", emojis: ["😊", "😄", "🤔", "😮", "😢", "😡", "🥳", "😎"] },
-  { category: "ジェスチャー", emojis: ["👏", "🙌", "🤝", "💪", "✌️", "🫡", "👋", "🙏"] },
-  { category: "テキスト", emojis: ["完了しました！", "了解しました！", "確認中です", "対応いたします", "ありがとうございます！", "お疲れ様です！", "禿同", "かしこ", "m9", "それな", "あざます", "orz"] },
+// kind: "text" は短文定型文（ボタン表示）、"emoji" は通常の絵文字グリッド表示
+export type EmojiGroup = { kind: "emoji" | "text"; category: string; emojis: string[] };
+
+export const EMOJI_LIST: EmojiGroup[] = [
+  { kind: "emoji", category: "よく使う", emojis: ["👍", "❤️", "😂", "🎉", "🔥", "👀", "💯", "✅", "🚀"] },
+  { kind: "emoji", category: "表情", emojis: ["😊", "😄", "🤔", "😮", "😢", "😡", "🥳", "😎"] },
+  { kind: "emoji", category: "ジェスチャー", emojis: ["👏", "🙌", "🤝", "💪", "✌️", "🫡", "👋", "🙏"] },
+  { kind: "text", category: "テキスト", emojis: ["完了しました！", "了解しました！", "確認中です", "対応いたします", "ありがとうございます！", "お疲れ様です！", "禿同", "かしこ", "m9", "それな", "あざます", "orz"] },
 ];
 
 export function EmojiPicker({ onSelect, onClose, position = "below" }: Props) {
@@ -70,7 +73,7 @@ export function EmojiPicker({ onSelect, onClose, position = "below" }: Props) {
           >
             {group.category}
           </p>
-          {group.category === "テキスト" ? (
+          {group.kind === "text" ? (
             <div className="flex flex-wrap gap-1">
               {group.emojis.map((emoji) => (
                 <button

```


## ステータス

✅ 修正適用完了
