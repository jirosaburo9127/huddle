# src/app/(workspace)/[workspace]/[channel]/components/channel-view.tsx (2026-05-13T08:18:37)

**種別**: Git diff (vs HEAD)


## 変更前 diff

```
diff --git a/src/app/(workspace)/[workspace]/[channel]/components/channel-view.tsx b/src/app/(workspace)/[workspace]/[channel]/components/channel-view.tsx
index 46bcb04..98b5539 100644
--- a/src/app/(workspace)/[workspace]/[channel]/components/channel-view.tsx
+++ b/src/app/(workspace)/[workspace]/[channel]/components/channel-view.tsx
@@ -1139,14 +1139,14 @@ export function ChannelView({ channel, initialMessages, currentUserId, initialLa
 
       // 「直近1週間を毎回フル取得 → ID で mergeById」が中抜けを起こさない唯一の正解。
       // 詳細と禁止パターン: AGENTS.md / src/lib/sync-fetcher.ts
-      // 直近2日分を再取得（Realtime + 復帰イベントで即時補完が効くため短縮）
-      // 長期間のバックグラウンド放置は稀なケースなので、2日で十分カバーできる
+      // 直近4日分を再取得。金曜夕方→月曜朝の業務パターンをカバーする。
+      // Realtime + 復帰イベントで即時補完も効くが、Realtimeが切れていた場合の保険。
       const fresh = await fetchSincePeriod<MessageWithProfile>({
         supabase,
         table: "messages",
         select: "*, profiles(*), reactions(*)",
         eq: { channel_id: channel.id },
-        sinceDays: 2,
+        sinceDays: 4,
         isCancelled: () => cancelled,
       });
 

```


## Codex レビュー

- [優先度: 今すぐ] `sinceDays: 4` が直前コメントの「直近1週間を毎回フル取得」「中抜けを起こさない唯一の正解」と矛盾しています。保証を維持するなら `sinceDays: 7` に戻してください。4日にするなら、この「唯一の正解」コメントと参照先の前提も同時に修正が必要です。

- [優先度: 今すぐ] 4日では連休・祝日・長期週末で Realtime が切れていた場合に中抜けします。コメントで「保険」と書くなら、少なくとも `sinceDays: 7` にして既存設計どおり直近1週間を再取得するのが安全です。

- [優先度: 後で] この挙動が重要なら、復帰時 fetch が期待期間で呼ばれることをテストしてください。例: `fetchSincePeriod` に渡る `sinceDays` が設計値どおりであること、期間外のメッセージを欠落させない前提が崩れないことを確認するテスト。


## 影響分析 (Claude read-only)

- **何が変わるか**: メッセージの取りこぼし対策で「最近何日分を再取得するか」を2日→4日に広げます。金曜夕方に画面を開きっぱなしで月曜朝に戻ってきても、その間の投稿が抜け落ちないようにする変更です。
- **影響範囲**: 変更箇所は ChannelView コンポーネント内の差分取得処理1ヶ所のみです。grepで `sinceDays` の利用箇所を確認したところ、この channel-view.tsx と共通関数 `src/lib/sync-fetcher.ts`（こちらは未変更）以外に該当はなく、他画面・他機能への影響はありません。サーバ問い合わせ範囲が2倍になりますが、Supabaseに直近4日分のメッセージを取りに行くだけで、ユーザー体験的にはむしろ抜け漏れが減る方向です。
- **LEVEL**: 軽微
- **根拠**: 変更は ChannelView 内の引数値のみで、共通関数や他コンポーネントには手を入れていない。


## ステータス

ユーザーが『スキップ』選択
