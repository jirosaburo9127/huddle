# src/app/(workspace)/[workspace]/[channel]/loading.tsx (2026-05-13T22:46:55)

**種別**: Git diff (vs HEAD)


## 変更前 diff

```
diff --git a/src/app/(workspace)/[workspace]/[channel]/loading.tsx b/src/app/(workspace)/[workspace]/[channel]/loading.tsx
index bc4bfde..63bf09f 100644
--- a/src/app/(workspace)/[workspace]/[channel]/loading.tsx
+++ b/src/app/(workspace)/[workspace]/[channel]/loading.tsx
@@ -1,4 +1,16 @@
+"use client";
+
+import { useEffect } from "react";
+import { useMobileNavStore } from "@/stores/mobile-nav-store";
+
 export default function ChannelLoading() {
+  const setSidebarOpen = useMobileNavStore((s) => s.setSidebarOpen);
+
+  // loading表示 = チャンネルへの遷移が始まった → サイドバーを閉じて詳細面をスライドイン
+  useEffect(() => {
+    setSidebarOpen(false);
+  }, [setSidebarOpen]);
+
   return (
     <div className="flex h-full flex-col bg-background">
       <header className="flex items-center px-3 sm:px-4 py-3 lg:py-0 lg:h-14 border-b border-border bg-header shrink-0">

```


## Codex レビュー

- [優先度: 今すぐ] `loading.tsx` に副作用を置くと、キャッシュ済み/即時完了するチャンネル遷移では `loading` がマウントされず、`setSidebarOpen(false)` が実行されません。サイドバーを閉じる処理は、チャンネルリンクのクリック処理、または `pathname` / route params の変更を監視する常駐クライアントコンポーネント側へ移してください。

- [優先度: 後で] この変更で `loading.tsx` 全体が Client Component になり、ローディング UI のためだけにストア依存とクライアント JS が増えます。`loading.tsx` は表示専用の Server Component のままにし、副作用だけを小さな別コンポーネントへ分離する方が責務が明確です。


## 影響分析 (Claude read-only)

- **何が変わるか**: モバイルでチャンネルを開いたときに、サイドバーを自動で閉じてチャンネル画面をスライドイン表示させる挙動を追加します。ただしレビュアー指摘の通り、キャッシュ済みの遷移ではこのloading画面が表示されないため、効かないケースが残ります。
- **影響範囲**: このloading.tsxはこのチャンネルページ専用で、他から呼ばれている関数はありません。`setSidebarOpen` 自体は sidebar / activity-modal / bottom-tab-bar / dm-list など多くの箇所で使われていますが、ここでは単に値をfalseにセットするだけなので他箇所の動作に影響しません。grep結果でも外部参照される export は無く、ChannelLoading は Next.js のルーティングが自動的に使う規約ファイルです。
- **LEVEL**: 軽微
- **根拠**: ChannelLoading は外部 export 用途がなく、変更内容も自分のサイドバー状態を閉じるだけで他機能に副作用なし。


## ステータス

ユーザーが『スキップ』選択
