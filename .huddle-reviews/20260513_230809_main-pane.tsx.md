# src/components/main-pane.tsx (2026-05-13T23:16:01)

**種別**: ファイル全体


## 変更前 diff

```
"use client";

import { useEffect, useRef, useState } from "react";
import { useMobileNavStore } from "@/stores/mobile-nav-store";

/**
 * ワークスペース main 領域のクライアント側ラッパー。
 * モバイルでは下部 BottomTabBar の高さ分の padding (pb-14) を確保するが、
 * メッセージ入力欄フォーカス中はバーが画面外にスライドするので padding も解除して
 * 余白が残らないようにする。
 */
export function MainPane({ children }: { children: React.ReactNode }) {
  const messageInputFocused = useMobileNavStore((s) => s.messageInputFocused);
  const sidebarOpen = useMobileNavStore((s) => s.sidebarOpen);
  const detailEnterVersion = useMobileNavStore((s) => s.detailEnterVersion);
  const [playingDetailEnter, setPlayingDetailEnter] = useState(false);
  const seenEnterVersionRef = useRef(0);

  useEffect(() => {
    if (detailEnterVersion === 0 || detailEnterVersion === seenEnterVersionRef.current) return;
    seenEnterVersionRef.current = detailEnterVersion;
    // animation再生を即開始（rAFを挟まない → 1フレーム目からfrom状態で始まる）
    setPlayingDetailEnter(true);
    const timer = setTimeout(() => setPlayingDetailEnter(false), 300);
    return () => {
      clearTimeout(timer);
    };
  }, [detailEnterVersion]);

  // animation再生中はtranslate-x-*とtransitionを無効化して
  // keyframe animationのtransformに完全に任せる
  const translateClass = playingDetailEnter
    ? "pointer-events-auto"
    : sidebarOpen
      ? "translate-x-full pointer-events-none lg:pointer-events-auto"
      : "translate-x-0 pointer-events-auto";

  return (
    <main
      className={`
        fixed inset-0 z-50 w-full bg-background flex flex-col min-w-0 transform-gpu
        lg:static lg:z-auto lg:flex-1 lg:translate-x-0 lg:transform-none lg:pb-0
        ${playingDetailEnter ? "animate-mobile-detail-enter" : "transition-[transform,padding] duration-200 ease-out"}
        ${translateClass}
        ${messageInputFocused ? "pb-0" : "pb-14"}
      `}
    >
      {children}
    </main>
  );
}

```


## Codex レビュー

- [優先度: 今すぐ] `playingDetailEnter` 中は `translateClass` が `"pointer-events-auto"` 固定になり、`sidebarOpen === true` でも `translate-x-full pointer-events-none` が外れます。300ms の間 main が前面で操作可能になり得るので、`sidebarOpen` 時の translate / pointer-events は常に維持するか、アニメーション開始条件を `!sidebarOpen` に限定してください。

- [優先度: 後で] `setTimeout(..., 300)` が CSS の `animate-mobile-detail-enter` の duration と暗黙に結合しています。CSS 側の時間を変えると状態解除タイミングがズレるため、共通定数化するか `onAnimationEnd` で `setPlayingDetailEnter(false)` する実装に寄せると壊れにくいです。

- [優先度: 後で] この挙動はストア値とタイマーに依存しているため、`detailEnterVersion` 更新時に `animate-mobile-detail-enter` が付与され、タイマー後に外れること、`sidebarOpen` 時に pointer-events / translate が崩れないことをコンポーネントテストで押さえてください。


## 影響分析 (Claude read-only)

- **何が変わるか**: モバイルで一覧→詳細に遷移するときの横スライドアニメ周りを整える変更です。アニメ中はCSS側の動きを邪魔しないように一時的にズレ補正を外し、終了したら元に戻します。
- **影響範囲**: MainPane は sidebar-loader.tsx から `<MainPane>{children}</MainPane>` の形で1か所だけ使われています (src/components/sidebar-loader.tsx:7,125)。内部の `playingDetailEnter` / `seenEnterVersionRef` は同ファイル内クロージャで、外部参照はありません。`detailEnterVersion` は mobile-nav-store と本ファイルのみ、`animate-mobile-detail-enter` は globals.css と本ファイルのみで参照されています。
- **LEVEL**: 軽微
- **根拠**: 変更はすべて MainPane コンポーネント内部の表示挙動に閉じており、外部export された関数や定数は変えていないため。


## ステータス

ユーザーが『スキップ』選択
