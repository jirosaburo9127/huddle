# src/lib/supabase/types.ts (2026-05-18T14:11:20)

**種別**: Git diff (vs HEAD)


## 変更前 diff

```
diff --git a/src/lib/supabase/types.ts b/src/lib/supabase/types.ts
index d1dac4f..3ccea3d 100644
--- a/src/lib/supabase/types.ts
+++ b/src/lib/supabase/types.ts
@@ -99,3 +99,31 @@ export type BoardNote = {
 export type BoardNoteWithProfile = BoardNote & {
   profiles?: Profile | null;
 };
+
+// アルバム
+export type Album = {
+  id: string;
+  channel_id: string;
+  title: string;
+  cover_url: string | null;
+  created_by: string;
+  created_at: string;
+};
+
+export type AlbumItem = {
+  id: string;
+  album_id: string;
+  url: string;
+  file_type: string;
+  file_name: string | null;
+  added_by: string;
+  created_at: string;
+};
+
+export type AlbumSummary = Album & {
+  channel_name: string;
+  channel_slug: string;
+  creator_name: string;
+  item_count: number;
+  first_item_url: string | null;
+};

```


## Codex レビュー

- [優先度: 後で] `AlbumItem.file_type: string` は値域が広すぎます。UI 側で画像/動画などの分岐に使うなら、`'image' | 'video' | 'audio' | 'document'` など実際の許容値に絞るか、Supabase の enum 型に合わせてください。

- [優先度: 後で] `AlbumSummary` の `channel_name` / `channel_slug` / `creator_name` が常に存在する前提になっています。JOIN 元が削除済み・NULL 許容・LEFT JOIN の可能性があるなら `string | null` にしてください。実態が INNER JOIN で NOT NULL ならこのままで問題ありません。

- [優先度: 任意] `AlbumItem.url` / `Album.cover_url` / `first_item_url` は外部表示・ダウンロードに使われる可能性が高いので、アプリ側で扱いやすいように `storage_path` と `public_url` のどちらを格納する設計か型名で明確にすると事故を減らせます。例: `storage_path` / `cover_storage_path`。


## 影響分析 (Claude read-only)

- **何が変わるか**: アルバム機能（チャンネル内に写真・動画をまとめて保存・一覧する仕組み）で扱うデータの設計図を新しく追加します。これ自体は画面の見た目や動作を変えるものではなく、これからアルバム機能を作るための土台を用意するだけです。
- **影響範囲**: 追加した Album / AlbumItem / AlbumSummary の3つを全ソースコードで検索しましたが、定義した types.ts 以外からは一切参照されていません（ヒットしたのは過去のレビュー記録ファイルと、無関係な既存のボード用 BoardNoteWithProfile だけ）。既存のメッセージ・通知・サイドバー・ボード等の機能には影響しません。
- **LEVEL**: 影響なし
- **根拠**: 3つの型はすべて新規追加で、types.ts 内に定義があるのみ・他ファイルからの利用が皆無のため。


## ステータス

ユーザーが『スキップ』選択
