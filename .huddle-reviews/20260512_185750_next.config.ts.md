# next.config.ts (2026-05-12T19:01:16)

**種別**: Git diff (vs HEAD)


## 変更前 diff

```
diff --git a/next.config.ts b/next.config.ts
index ee3d36a..0dea757 100644
--- a/next.config.ts
+++ b/next.config.ts
@@ -3,9 +3,8 @@ import type { NextConfig } from "next";
 const nextConfig: NextConfig = {
   poweredByHeader: false,
   serverExternalPackages: ["@supabase/supabase-js", "@supabase/ssr"],
-  // チャンネル切替時に前のチャンネルが表示される問題を防ぐため、
-  // 動的ページのキャッシュを無効にして loading.tsx スケルトンを即座に表示する。
-  // データ鮮度は syncMissedMessages が補正する。
+  // 動的ページのキャッシュは持たず、チャンネル遷移時の古い初期データ表示を避ける。
+  // route-level loading は置かず、準備中に画面全体をグレースケルトンへ差し替えない。
   experimental: {
     staleTimes: {
       dynamic: 0,

```


## Codex レビュー

- [優先度: 任意] `next.config.ts:5` のコメントに「route-level loading は置かず」とありますが、この設定ファイル自体では `loading.tsx` の有無を保証できません。実装と乖離しやすいので、「`staleTimes.dynamic = 0` により動的ページのクライアントキャッシュを持たない」程度の設定内容に限定したコメントへ直すと安全です。


## 影響分析 (Claude read-only)

- **何が変わるか**: チャンネルなどのページを切り替えたときに、前のページの古い中身が一瞬残って見える問題を防ぐ設定です。今回はコードの動きは変わらず、コメント(注釈)の文章を実態に合わせて整えただけです。
- **影響範囲**: next.config.ts の設定値そのものは変更しておらず、編集箇所はコメント文だけです。staleTimes はこのファイル以外で参照されておらず（grep 済み）、loading.tsx もプロジェクト内に存在しません（glob で 0件）。他ファイル・他機能への影響はありません。
- **LEVEL**: 影響なし
- **根拠**: コメント文言の修正のみで、実行時の挙動や他ファイルからの参照に変化なし。


## Claude Code 修正 (応答)

採用。指摘どおり、next.config.ts のコメントは設定内容（`staleTimes.dynamic = 0`）に限定し、`loading.tsx` の方針記述は削除しました。実装と乖離する可能性を排除。


## 修正後 diff (vs HEAD)

```
diff --git a/next.config.ts b/next.config.ts
index ee3d36a..1980c9a 100644
--- a/next.config.ts
+++ b/next.config.ts
@@ -3,9 +3,7 @@ import type { NextConfig } from "next";
 const nextConfig: NextConfig = {
   poweredByHeader: false,
   serverExternalPackages: ["@supabase/supabase-js", "@supabase/ssr"],
-  // チャンネル切替時に前のチャンネルが表示される問題を防ぐため、
-  // 動的ページのキャッシュを無効にして loading.tsx スケルトンを即座に表示する。
-  // データ鮮度は syncMissedMessages が補正する。
+  // staleTimes.dynamic = 0 により、動的ページのクライアントサイドキャッシュを持たない。
   experimental: {
     staleTimes: {
       dynamic: 0,

```


## ステータス

✅ 修正適用完了
