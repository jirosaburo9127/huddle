# src/lib/supabase/types.ts (2026-05-15T20:18:09)

**種別**: Git diff (vs HEAD)


## 変更前 diff

```
diff --git a/src/lib/supabase/types.ts b/src/lib/supabase/types.ts
index 832d7ae..d1dac4f 100644
--- a/src/lib/supabase/types.ts
+++ b/src/lib/supabase/types.ts
@@ -74,3 +74,28 @@ export type MessageWithProfile = Message & {
   profiles: Profile;
   reactions?: Reaction[];
 };
+
+// 付箋ボード
+export type Board = {
+  id: string;
+  workspace_id: string;
+  title: string;
+  created_by: string;
+  is_active: boolean;
+  created_at: string;
+  closed_at: string | null;
+};
+
+export type BoardNote = {
+  id: string;
+  board_id: string;
+  user_id: string;
+  content: string;
+  category: string | null;
+  color: string;
+  created_at: string;
+};
+
+export type BoardNoteWithProfile = BoardNote & {
+  profiles?: Profile | null;
+};

```


## Codex レビュー

- [優先度: 後で] `Board` / `BoardNote` が Supabase の生成型から手書きで追加されているため、DB スキーマ変更時に型がズレやすいです。既存方針が許すなら `Database['public']['Tables']['boards']['Row']` のような生成型エイリアスに寄せてください。

- [優先度: 後で] `BoardNote.color: string` は任意文字列だと UI 側で不正な色値や想定外テーマを通せます。保存可能な色が固定なら `type BoardNoteColor = 'yellow' | ...` のような union 型にしてください。

- [優先度: 任意] `BoardNote.category: string | null` もカテゴリ候補が固定なら union 型化すると、表示分岐や集計時の typo を防げます。自由入力なら現状で問題ありません。


## 影響分析 (Claude read-only)

- **何が変わるか**: 「付箋ボード」機能で使うデータの形（ボード本体と付箋メモ）をプログラムが扱えるように定義を追加します。これ自体は画面の見た目や動きを変えるものではなく、今後この機能を作るための土台です。
- **影響範囲**: Board / BoardNote / BoardNoteWithProfile を全ソースコード（.ts / .tsx）で検索しましたが、定義した types.ts ファイル以外からは一切呼ばれていません。新規追加の型で、まだどの画面・機能も使っていないため、既存のメッセージ・通知・サイドバー等への影響はありません。
- **LEVEL**: 影響なし
- **根拠**: 3つの型はいずれも新規追加で、定義ファイル内にのみ存在し外部参照ゼロ。


## Claude Code 修正 (応答)

NO_FIX

理由:
1. **生成型エイリアス**: このファイルは冒頭コメント(1-2行目)通り全型を手書きで運用する明示的方針。`Database` 生成型自体が存在せず、寄せると既存規約を破る。
2. **color union**: `note.color` は UI で未使用（`sticky-note.tsx` は `category` のハッシュで色決定）。書き込み側も無く実値は migration デフォルト `'yellow'` のみで、union 化は推測になる。
3. **category union**: AI 自動分類フィールド（未分類時「分類中...」表示）で自由入力。union 化は不適切。

3点とも本質的でないと判断し、変更なし。


## ステータス

Claude Code が NO_FIX 判定
