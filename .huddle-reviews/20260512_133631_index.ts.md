# supabase/functions/send-vote-push/index.ts (2026-05-12T13:36:56)

**種別**: Git diff (vs HEAD)


## 変更前 diff

```
diff --git a/supabase/functions/send-vote-push/index.ts b/supabase/functions/send-vote-push/index.ts
index b6bf34e..b58e147 100644
--- a/supabase/functions/send-vote-push/index.ts
+++ b/supabase/functions/send-vote-push/index.ts
@@ -125,7 +125,9 @@ Deno.serve(async (req) => {
         ? (channel as unknown as { workspaces: { slug: string }[] }).workspaces[0]?.slug
         : (channel as unknown as { workspaces: { slug: string } }).workspaces.slug);
     const channelUrl =
-      workspaceSlug && channel.slug ? `/${workspaceSlug}/${channel.slug}` : "/";
+      workspaceSlug && channel.slug
+        ? `/${workspaceSlug}/${channel.slug}?m=${poll.message_id}`
+        : "/";
 
     // 通知先: 投票作成者
     const recipientId = poll.created_by;

```


## Codex レビュー

- [優先度: 今すぐ] `poll.message_id` が `null` / `undefined` の場合に `?m=null` や `?m=undefined` が入る可能性があります。`poll.message_id` が存在する場合だけクエリを付け、なければ従来通り `/${workspaceSlug}/${channel.slug}` にしてください。

- [優先度: 今すぐ] `workspaceSlug` / `channel.slug` / `poll.message_id` をURLに直埋めしています。slugやmessage_idに `/`, `?`, `#`, `&` などが混じるとURLが壊れるため、各セグメントは `encodeURIComponent`、クエリは `URLSearchParams` で組み立ててください。

- [優先度: 後で] テストがあるなら、`message_id` あり・なし、特殊文字を含む `message_id`、`workspaceSlug` または `channel.slug` 欠落時に期待URLになるケースを追加してください。


## 影響分析 (Claude read-only)

- **何が変わるか**: 投票通知をタップした時に、投票が紐づくメッセージの位置まで自動でジャンプできるようにします。今は対象チャンネルを開くだけで、どのメッセージへの投票か探す必要がありました。
- **影響範囲**: 変更は send-vote-push Edge Function（投票通知の配信処理）の URL 生成部分のみで、他のファイルからは参照されていません（grep で send-vote-push の参照は同関数内とマイグレーション定義のみ）。生成された URL を実際に処理する push-tap-handler.tsx 側は `?m=...` 形式に既に対応済みです（send-push / send-reaction-push と同じ形式）。ただし指摘の通り、`poll.message_id` が null の場合に `?m=null` が入る可能性と、slug に特殊文字が混じった場合にURLが壊れる可能性があり、修正が必要です。
- **LEVEL**: 軽微
- **根拠**: 影響範囲は当該 Edge Function 内の URL 文字列生成のみで、外部呼び出し側は同形式に既対応のため。


## Claude Code 修正 (応答)

採用した指摘:
- `poll.message_id` が無い場合は `?m=` を付けない（採用）
- `workspaceSlug` / `channel.slug` / `message_id` を `encodeURIComponent` + `URLSearchParams` でエンコード（採用）

不採用:
- テスト追加: このリポジトリに該当 Edge Function のテストが未整備のため今回はスコープ外。

変更要約:
- `channelUrl` 組み立てを IIFE 化し、`message_id` 有無で分岐。
- 各セグメントを `encodeURIComponent`、クエリは `URLSearchParams` で生成。
- 既存の `workspaceSlug`/`channel.slug` 欠落時のフォールバック `/` は維持。


## 修正後 diff (vs HEAD)

```
diff --git a/supabase/functions/send-vote-push/index.ts b/supabase/functions/send-vote-push/index.ts
index b6bf34e..8b453f4 100644
--- a/supabase/functions/send-vote-push/index.ts
+++ b/supabase/functions/send-vote-push/index.ts
@@ -124,8 +124,13 @@ Deno.serve(async (req) => {
       )
         ? (channel as unknown as { workspaces: { slug: string }[] }).workspaces[0]?.slug
         : (channel as unknown as { workspaces: { slug: string } }).workspaces.slug);
-    const channelUrl =
-      workspaceSlug && channel.slug ? `/${workspaceSlug}/${channel.slug}` : "/";
+    const channelUrl = (() => {
+      if (!workspaceSlug || !channel.slug) return "/";
+      const base = `/${encodeURIComponent(workspaceSlug)}/${encodeURIComponent(channel.slug)}`;
+      if (!poll.message_id) return base;
+      const qs = new URLSearchParams({ m: String(poll.message_id) }).toString();
+      return `${base}?${qs}`;
+    })();
 
     // 通知先: 投票作成者
     const recipientId = poll.created_by;

```


## ステータス

✅ 修正適用完了
