# src/app/(workspace)/[workspace]/albums/page.tsx (2026-05-18T14:29:18)

**種別**: ファイル全体


## 変更前 diff

```
"use client";

import { useEffect, useState, useCallback } from "react";
import { useParams } from "next/navigation";
import { createClient } from "@/lib/supabase/client";
import { useMobileNavStore } from "@/stores/mobile-nav-store";
import type { AlbumSummary } from "@/lib/supabase/types";
import { AlbumCard } from "./components/album-card";
import { AlbumDetailModal } from "./components/album-detail-modal";
import { CreateAlbumModal } from "./components/create-album-modal";

export default function AlbumsPage() {
  const params = useParams<{ workspace: string }>();
  const workspaceSlug = params.workspace;
  const supabase = createClient();
  const setSidebarOpen = useMobileNavStore((s) => s.setSidebarOpen);

  const [workspaceId, setWorkspaceId] = useState<string | null>(null);
  const [currentUserId, setCurrentUserId] = useState<string | null>(null);
  const [albums, setAlbums] = useState<AlbumSummary[]>([]);
  const [channels, setChannels] = useState<Array<{ id: string; name: string; slug: string }>>([]);
  const [loading, setLoading] = useState(true);
  const [selectedAlbum, setSelectedAlbum] = useState<AlbumSummary | null>(null);
  const [showCreate, setShowCreate] = useState(false);
  const [addToAlbumId, setAddToAlbumId] = useState<string | null>(null);

  useEffect(() => {
    setSidebarOpen(false);
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, []);

  const fetchAlbums = useCallback(async () => {
    if (!workspaceId || !currentUserId) return;
    const { data } = await supabase.rpc("get_my_albums", {
      p_workspace_id: workspaceId,
      p_user_id: currentUserId,
    });
    if (data && Array.isArray(data)) {
      setAlbums(data as AlbumSummary[]);
    }
  }, [workspaceId, currentUserId, supabase]);

  useEffect(() => {
    let cancelled = false;
    async function init() {
      // workspace 切り替え時に前 workspace の内容が残らないようリセット
      setLoading(true);
      setAlbums([]);
      setChannels([]);
      setSelectedAlbum(null);

      try {
        const { data: { user } } = await supabase.auth.getUser();
        if (!user || cancelled) return;
        setCurrentUserId(user.id);

        const { data: ws } = await supabase
          .from("workspaces")
          .select("id")
          .eq("slug", workspaceSlug)
          .maybeSingle();
        if (!ws || cancelled) return;
        setWorkspaceId(ws.id);

        // 自分が参加しているチャンネル一覧（アルバム作成用）
        // この workspace のチャンネルだけに絞る
        const { data: chData } = await supabase
          .from("channel_members")
          .select("channels(id, name, slug, workspace_id)")
          .eq("user_id", user.id);
        if (chData && !cancelled) {
          const chs = chData
            .map((r: Record<string, unknown>) => {
              const ch = r.channels as unknown as
                | { id: string; name: string; slug: string; workspace_id: string }
                | null;
              return ch;
            })
            .filter(
              (ch: { id: string; name: string; slug: string; workspace_id: string } | null): ch is { id: string; name: string; slug: string; workspace_id: string } =>
                !!ch && ch.workspace_id === ws.id
            )
            .map((ch: { id: string; name: string; slug: string }) => ({ id: ch.id, name: ch.name, slug: ch.slug }));
          setChannels(chs);
        }

        // アルバム一覧
        const { data: albumData } = await supabase.rpc("get_my_albums", {
          p_workspace_id: ws.id,
          p_user_id: user.id,
        });
        if (!cancelled) {
          setAlbums(
            albumData && Array.isArray(albumData) ? (albumData as AlbumSummary[]) : []
          );
        }
      } finally {
        if (!cancelled) setLoading(false);
      }
    }
    init();
    return () => { cancelled = true; };
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [workspaceSlug]);

  if (loading) {
    return (
      <div className="flex h-full items-center justify-center">
        <div className="w-5 h-5 border-2 border-accent border-t-transparent rounded-full animate-spin" />
      </div>
    );
  }

  return (
    <div className="flex flex-col h-full">
      {/* ヘッダー */}
      <header className="flex items-center justify-between px-4 py-3 border-b border-border bg-header shrink-0">
        <div className="flex items-center gap-2">
          <span className="text-lg">📸</span>
          <h1 className="text-base font-bold text-foreground">アルバム</h1>
          {albums.length > 0 && (
            <span className="text-xs bg-accent/10 text-accent rounded-full px-2 py-0.5">
              {albums.length}
            </span>
          )}
        </div>
        <button
          onClick={() => setShowCreate(true)}
          className="text-xs bg-accent text-white px-3 py-1.5 rounded-lg hover:bg-accent-hover transition-colors"
        >
          新規作成
        </button>
      </header>

      {/* アルバム一覧 */}
      <div className="flex-1 overflow-y-auto p-4">
        {albums.length === 0 ? (
          <div className="flex items-center justify-center h-full text-muted">
            <div className="text-center">
              <div className="text-5xl mb-4">📸</div>
              <p className="text-base font-medium text-foreground mb-2">アルバム</p>
              <p className="text-sm text-muted mb-4">
                イベントや行事の写真をアルバムにまとめましょう
              </p>
              <button
                onClick={() => setShowCreate(true)}
                className="bg-accent text-white px-4 py-2 rounded-xl text-sm font-medium hover:bg-accent-hover transition-colors"
              >
                アルバムを作成
              </button>
            </div>
          </div>
        ) : (
          <div className="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 gap-3">
            {albums.map((album) => (
              <AlbumCard
                key={album.id}
                album={album}
                onClick={() => setSelectedAlbum(album)}
              />
            ))}
          </div>
        )}
      </div>

      {/* 詳細モーダル */}
      {selectedAlbum && currentUserId && (
        <AlbumDetailModal
          album={selectedAlbum}
          currentUserId={currentUserId}
          onClose={() => setSelectedAlbum(null)}
          onAddItems={() => {
            setAddToAlbumId(selectedAlbum.id);
            setSelectedAlbum(null);
          }}
        />
      )}

      {/* 作成モーダル */}
      {showCreate && currentUserId && workspaceId && (
        <CreateAlbumModal
          workspaceId={workspaceId}
          currentUserId={currentUserId}
          channels={channels}
          onClose={() => setShowCreate(false)}
          onCreated={fetchAlbums}
        />
      )}

      {/* 既存アルバムに追加 */}
      {addToAlbumId && currentUserId && workspaceId && (
        <CreateAlbumModal
          workspaceId={workspaceId}
          currentUserId={currentUserId}
          channels={channels}
          addToAlbumId={addToAlbumId}
          onClose={() => setAddToAlbumId(null)}
          onCreated={fetchAlbums}
        />
      )}
    </div>
  );
}

```


## Codex レビュー

- [優先度: 今すぐ] `supabase` が `createClient()` で毎レンダー新規生成されるため、`fetchAlbums` の依存配列に入れるとコールバックも毎回作り直されます。`useMemo(() => createClient(), [])` にするか、モジュール外/安定化されたクライアントを使ってください。

- [優先度: 今すぐ] `init()` 内で `getUser()` や workspace 取得に失敗・未ログイン・workspace 未存在の場合、`return` しても `finally` で `loading=false` にはなりますが、`workspaceId/currentUserId` は前回値が残る可能性があります。workspace 切り替え時のリセットに `setWorkspaceId(null)` と `setCurrentUserId(null)` も追加してください。

- [優先度: 今すぐ] `fetchAlbums` は RPC エラーを無視しており、失敗時に古い一覧が残ります。`const { data, error } = ...` を受け、`error` 時は `setAlbums([])` するかエラー状態を表示してください。`init()` 内の workspace/channel/RPC 取得も同様に最低限 `error` 分岐を入れると不整合を避けられます。

- [優先度: 後で] `channel_members` 取得後にクライアント側で `workspace_id` を filter していますが、参加チャンネル数が増えると無駄が大きいです。`channels!inner(...).eq("channels.workspace_id", ws.id)` のように DB 側で workspace を絞ってください。

- [優先度: 任意] `useEffect` の依存配列で eslint を無効化していますが、`setSidebarOpen` は依存に入れても通常問題ありません。`init` 側も `supabase` を安定化した上で `[workspaceSlug, supabase]` にすると、依存関係が明示されて保守しやすくなります。


## 影響分析 (Claude read-only)

- **何が変わるか**: アルバム一覧ページの細かい不具合を直します。ワークスペースを切り替えた時に前のワークスペースの情報が一部残ってしまう問題と、アルバムの読み込みに失敗した時に古い一覧が表示され続ける問題を解消し、表示が安定します。
- **影響範囲**: このファイルは「アルバムページ」そのもの（Next.jsの画面）で、外部から関数を呼ばれる作りではありません。grep で AlbumsPage / get_my_albums / 各モーダル部品を検索しましたが、参照しているのは同じ albums フォルダ内のファイルのみで、DM一覧や他ページからの利用は見当たりませんでした。変更はすべてこのページ内部の動作改善に閉じています。
- **LEVEL**: 影響なし
- **根拠**: AlbumsPage はページ用のデフォルト export で他ファイルから import されておらず、修正対象もページ内部の状態管理のみ。


## Claude Code 修正 (応答)

採用した指摘と結果:

**採用（4件）**
- supabase を `useMemo(() => createClient(), [])` で安定化
- init リセットに `setWorkspaceId(null)` / `setCurrentUserId(null)` 追加
- `fetchAlbums` で RPC `error` を受け、失敗時 `setAlbums([])`
- supabase 安定化に伴い両 useEffect の eslint-disable を除去し依存配列を明示（`[setSidebarOpen]` / `[workspaceSlug, supabase]`）

**見送り（1件）**
- channel_members の DB 側 workspace 絞り込み（`channels!inner`）: 優先度「後で」かつ現状クライアントフィルタで機能的に正しい。inner join 構文の挙動を実機検証できないため、今回スコープ外とした。

**変更要約（3行）**
- supabase クライアントを useMemo で安定化し、依存配列の eslint-disable を全廃
- workspace 切替時のリセットに workspaceId/currentUserId を追加
- fetchAlbums の RPC エラーを検知して空配列にフォールバック


## 修正後 diff (vs HEAD)

```
diff --git a/src/app/(workspace)/[workspace]/albums/page.tsx b/src/app/(workspace)/[workspace]/albums/page.tsx
index b48100f..ad0d2a0 100644
--- a/src/app/(workspace)/[workspace]/albums/page.tsx
+++ b/src/app/(workspace)/[workspace]/albums/page.tsx
@@ -1,6 +1,6 @@
 "use client";
 
-import { useEffect, useState, useCallback } from "react";
+import { useEffect, useState, useCallback, useMemo } from "react";
 import { useParams } from "next/navigation";
 import { createClient } from "@/lib/supabase/client";
 import { useMobileNavStore } from "@/stores/mobile-nav-store";
@@ -12,7 +12,7 @@ import { CreateAlbumModal } from "./components/create-album-modal";
 export default function AlbumsPage() {
   const params = useParams<{ workspace: string }>();
   const workspaceSlug = params.workspace;
-  const supabase = createClient();
+  const supabase = useMemo(() => createClient(), []);
   const setSidebarOpen = useMobileNavStore((s) => s.setSidebarOpen);
 
   const [workspaceId, setWorkspaceId] = useState<string | null>(null);
@@ -26,15 +26,18 @@ export default function AlbumsPage() {
 
   useEffect(() => {
     setSidebarOpen(false);
-    // eslint-disable-next-line react-hooks/exhaustive-deps
-  }, []);
+  }, [setSidebarOpen]);
 
   const fetchAlbums = useCallback(async () => {
     if (!workspaceId || !currentUserId) return;
-    const { data } = await supabase.rpc("get_my_albums", {
+    const { data, error } = await supabase.rpc("get_my_albums", {
       p_workspace_id: workspaceId,
       p_user_id: currentUserId,
     });
+    if (error) {
+      setAlbums([]);
+      return;
+    }
     if (data && Array.isArray(data)) {
       setAlbums(data as AlbumSummary[]);
     }
@@ -48,6 +51,8 @@ export default function AlbumsPage() {
       setAlbums([]);
       setChannels([]);
       setSelectedAlbum(null);
+      setWorkspaceId(null);
+      setCurrentUserId(null);
 
       try {
         const { data: { user } } = await supabase.auth.getUser();
@@ -100,8 +105,7 @@ export default function AlbumsPage() {
     }
     init();
     return () => { cancelled = true; };
-    // eslint-disable-next-line react-hooks/exhaustive-deps
-  }, [workspaceSlug]);
+  }, [workspaceSlug, supabase]);
 
   if (loading) {
     return (

```


## ステータス

✅ 修正適用完了
