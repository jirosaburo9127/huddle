# src/components/main-pane.tsx (2026-05-13T23:26:36)

**種別**: ファイル全体


## 変更前 diff

```
"use client";

import { useEffect, useRef, useState } from "react";
import { useMobileNavStore } from "@/stores/mobile-nav-store";

/**
 * ワークスペース main 領域のクライアント側ラッパー。
 * モバイルでは下部 BottomTabBar の高さ分の padding (pb-14) を確保するが、
 * メッセージ入力欄フォーカス中はバーが画面外にスライドするので padding も解除して
 * 余白が残らないようにする。
 */
export function MainPane({ children }: { children: React.ReactNode }) {
  const messageInputFocused = useMobileNavStore((s) => s.messageInputFocused);
  const sidebarOpen = useMobileNavStore((s) => s.sidebarOpen);
  const setSidebarOpen = useMobileNavStore((s) => s.setSidebarOpen);
  const detailEnterVersion = useMobileNavStore((s) => s.detailEnterVersion);
  const [playingDetailEnter, setPlayingDetailEnter] = useState(false);
  const seenEnterVersionRef = useRef(0);

  useEffect(() => {
    if (detailEnterVersion === 0 || detailEnterVersion === seenEnterVersionRef.current) return;
    seenEnterVersionRef.current = detailEnterVersion;
    setPlayingDetailEnter(true);
    // アニメーション完了後にsidebarを閉じる（translate-x-0に確定）
    const timer = setTimeout(() => {
      setPlayingDetailEnter(false);
      setSidebarOpen(false);
    }, 250);
    return () => {
      clearTimeout(timer);
    };
  }, [detailEnterVersion, setSidebarOpen]);

  // animation再生中はsidebarOpenのtranslateクラスを無視して
  // keyframe animationのtransformに完全に任せる
  const translateClass = playingDetailEnter
    ? "pointer-events-auto"
    : sidebarOpen
      ? "translate-x-full pointer-events-none lg:pointer-events-auto"
      : "translate-x-0 pointer-events-auto";

  return (
    <main
      className={`
        fixed inset-0 z-50 w-full bg-background flex flex-col min-w-0 transform-gpu
        lg:static lg:z-auto lg:flex-1 lg:translate-x-0 lg:transform-none lg:pb-0
        ${playingDetailEnter ? "animate-mobile-detail-enter" : "transition-[transform,padding] duration-200 ease-out"}
        ${translateClass}
        ${messageInputFocused ? "pb-0" : "pb-14"}
      `}
    >
      {children}
    </main>
  );
}

```


## Codex レビュー

- [優先度: 今すぐ] `detailEnterVersion` が短時間に連続更新された場合、`playingDetailEnter` がすでに `true` のため `animate-mobile-detail-enter` クラスが付け直されず、2回目以降の keyframe が再開始しない可能性があります。`animationKey` state を持って `<main key>` 相当ではなく `style={{ animationName: ... }}` の再適用や、`setPlayingDetailEnter(false)` を挟むなどで再トリガーしてください。

- [優先度: 今すぐ] `setTimeout(..., 250)` が CSS アニメーション時間とハードコードで結合しています。`animate-mobile-detail-enter` の duration を変更すると `setSidebarOpen(false)` のタイミングがずれます。`onAnimationEnd` で閉じる、または duration 定数を CSS/TS で共有してください。

- [優先度: 後で] `useEffect` が画面幅に関係なく動くため、デスクトップ表示中でも `detailEnterVersion` 更新で `sidebarOpen` を `false` にします。モバイル専用挙動なら media query 判定を入れるか、呼び出し側/store 側で mobile 時だけ `detailEnterVersion` を更新するようにしてください。

- [優先度: 後で] `translateClass` の三項演算子が状態遷移の意図を読み取りづらいです。`isMainHidden = sidebarOpen && !playingDetailEnter` のような中間変数に分けると、`pointer-events` と `translate` の対応が追いやすくなります。

- [優先度: 後で] テストがあるなら、`detailEnterVersion` 更新時に「アニメーション中は pointer-events 有効」「250ms/animation end 後に `setSidebarOpen(false)`」「入力フォーカス時に `pb-0` になる」をカバーしてください。


## 影響分析 (Claude read-only)

- **何が変わるか**: モバイルでチャンネル一覧から詳細画面へ切り替わるときのスライドインアニメをより確実に動くようにする変更です。アニメ中はタップ操作も効くようになり、終了後にサイドバー状態を正しく閉じます。
- **影響範囲**: MainPane は sidebar-loader.tsx の1か所からのみ呼ばれています (src/components/sidebar-loader.tsx:7,125)。内部で使う playingDetailEnter / seenEnterVersionRef は同ファイル内クロージャで外部参照なし。連動する detailEnterVersion は mobile-nav-store と本ファイルのみ、animate-mobile-detail-enter クラスは globals.css と本ファイルのみで使われており、他機能 (DM、ワークスペース設定、デスクトップ表示) への波及はありません。
- **LEVEL**: 軽微
- **根拠**: 変更は MainPane 内部のクロージャに閉じており、利用箇所も sidebar-loader の1か所のみ。


## ステータス

ユーザーが『スキップ』選択
