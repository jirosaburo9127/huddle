# src/app/(workspace)/[workspace]/board/components/board-canvas.tsx (2026-05-16T05:47:05)

**種別**: ファイル全体


## 変更前 diff

```
"use client";

import { useMemo } from "react";
import type { BoardNoteWithProfile } from "@/lib/supabase/types";
import { StickyNote, categoryColor } from "./sticky-note";

type Props = {
  notes: BoardNoteWithProfile[];
};

// ノートIDから擬似ランダムな位置・回転を決定（同じIDなら同じ位置）
function notePosition(id: string, index: number, total: number) {
  let hash = 0;
  for (let i = 0; i < id.length; i++) {
    hash = ((hash << 5) - hash + id.charCodeAt(i)) | 0;
  }
  const h = Math.abs(hash);
  // 画面を均等に埋めるため、indexベースのグリッド位置にランダムオフセットを加える
  const cols = Math.max(4, Math.ceil(Math.sqrt(total * 1.5)));
  const row = Math.floor(index / cols);
  const col = index % cols;
  const cellW = 100 / cols;
  const cellH = cellW * 1.2;
  const left = col * cellW + (h % 30) * cellW * 0.01;
  const top = row * cellH + ((h >> 8) % 30) * cellH * 0.01;
  const rotate = ((h % 13) - 6) * 1.2; // -7.2deg ~ +7.2deg
  return { left: `${Math.min(left, 95)}%`, top: `${top}%`, rotate: `${rotate}deg` };
}

export function BoardCanvas({ notes }: Props) {
  // カテゴリ一覧（凡例用）
  const categories = useMemo(() => {
    const set = new Set<string>();
    for (const n of notes) {
      if (n.category) set.add(n.category);
    }
    return [...set];
  }, [notes]);

  if (notes.length === 0) {
    return (
      <div className="flex-1 flex items-center justify-center text-muted px-6">
        <div className="text-center">
          <div className="text-4xl mb-3">📝</div>
          <p className="text-sm">まだ付箋がありません</p>
          <p className="text-xs mt-1 text-muted/70">下の入力欄からアイディアを投稿してください</p>
        </div>
      </div>
    );
  }

  return (
    <div className="flex-1 overflow-auto relative">
      {/* カテゴリ凡例 */}
      {categories.length > 0 && (
        <div className="sticky top-0 z-10 flex flex-wrap gap-2 px-4 py-2 bg-background/80 backdrop-blur-sm">
          {categories.map((cat) => (
            <span
              key={cat}
              className={`text-[10px] px-2 py-0.5 text-gray-700 font-medium ${categoryColor(cat)}`}
            >
              {cat}
            </span>
          ))}
        </div>
      )}

      {/* 付箋が散らばるエリア */}
      <div className="relative" style={{ minHeight: Math.max(500, Math.ceil(notes.length / 4) * 160) }}>
        {notes.map((note, i) => {
          const pos = notePosition(note.id, i, notes.length);
          return (
            <div
              key={note.id}
              className="absolute transition-all duration-500 hover:z-10 hover:scale-110"
              style={{
                left: pos.left,
                top: pos.top,
                transform: `rotate(${pos.rotate})`,
              }}
            >
              <StickyNote note={note} />
            </div>
          );
        })}
      </div>
    </div>
  );
}

```


## Codex レビュー

- [優先度: 今すぐ] `notePosition()` が `index` と `total` に依存しているため、ノート追加・削除・並び順変更のたびに既存ノートの位置が変わります。コメントの「同じIDなら同じ位置」とも不一致です。位置を安定させたいなら、`id` の hash だけで `left/top/rotate` を決めるか、配置前に安定キーで sort してください。

- [優先度: 今すぐ] `className` の `hover:scale-110` は、inline style の `transform: rotate(...)` に上書きされて効きません。`style={{ rotate: pos.rotate }}` のように CSS individual transform property を使うか、hover 時の scale も含めた transform を CSS 変数/Tailwind 側で合成してください。

- [優先度: 後で] `left: Math.min(left, 95)%` は付箋自体の幅を考慮していないため、右端付近の付箋が横スクロールや見切れを起こしやすいです。`calc(100% - NOTE_WIDTH)` 相当の制約、またはグリッドセル内で `left/top` を px/percent 混在せず計算する実装にすると安定します。

- [優先度: 任意] `useMemo` の `categories` は重い処理ではないので必須ではありません。残すならカテゴリ表示順が `notes` の順序に依存するため、UIの揺れを避けたい場合は `return [...set].sort()` にすると見通しが良くなります。


## 影響分析 (Claude read-only)

- **何が変わるか**: 付箋ボードの見た目を安定させる修正です。今は付箋を1枚足したり消したりするたびに、関係ない既存の付箋まで位置がバラバラに動いてしまいます。これを「同じ付箋はいつも同じ場所・同じ傾き」に直します。あわせて、付箋にマウスを乗せても拡大されない不具合と、右端の付箋が画面からはみ出る問題も改善します。
- **影響範囲**: 位置を計算する `notePosition` 関数はこのファイル内だけで使われており（grep で定義1か所・呼び出し1か所のみ、外部 export なし）、他画面に波及しません。このボード画面の本体 `BoardCanvas` は board-layout.tsx が `<BoardCanvas notes={notes} />` の形で呼ぶだけで、受け渡す情報の形は変わらないため呼び出し側の修正は不要です。付箋の見た目部品 `StickyNote`・色分け `categoryColor` はこのファイルが利用しているだけで、今回それ自体は変更しません。
- **LEVEL**: 軽微
- **根拠**: notePosition は外部未公開でこのファイル内専用、BoardCanvas の受け渡し仕様も不変のため。


## ステータス

ユーザーが『スキップ』選択
