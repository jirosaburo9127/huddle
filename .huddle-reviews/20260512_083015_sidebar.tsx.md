# src/components/sidebar.tsx (2026-05-12T09:00:16)

**種別**: Git diff (vs HEAD)


## 変更前 diff

```
diff --git a/src/components/sidebar.tsx b/src/components/sidebar.tsx
index 789ad49..86c14a0 100644
--- a/src/components/sidebar.tsx
+++ b/src/components/sidebar.tsx
@@ -1320,6 +1320,7 @@ export function Sidebar({
         <ActivityModal
           workspaceSlug={workspaceSlug}
           workspaceId={workspace.id}
+          currentChannelSlug={pathname.split("/")[2] || null}
           onClose={() => setShowActivity(false)}
         />
       )}

```


## Codex レビュー

- [優先度: 今すぐ] `src/components/sidebar.tsx:1323` の `pathname.split("/")[2]` はルート構造に強く依存していて、`/workspace/settings` や `/workspace/members` のような非チャンネル画面でも `"settings"` などを `currentChannelSlug` として渡す可能性があります。チャンネル一覧に存在する slug か検証してから渡す、または既存の選択中チャンネル state/route params から取得してください。

- [優先度: 今すぐ] チャンネル URL が `/workspace/channels/:slug` のような階層の場合、`split("/")[2]` は実際の slug ではなく中間セグメントになります。ルート定義に合わせた helper を作るか、Next の `useParams()` 等で named param から取得する形にしてください。

- [優先度: 後で] `currentChannelSlug` 抽出ロジックの単体テストを追加してください。少なくとも「チャンネル画面では slug を返す」「非チャンネル画面では `null`」「ネストしたパスで誤ったセグメントを返さない」を押さえると、この種の回帰を防げます。


## 影響分析 (Claude read-only)

- **何が変わるか**: アクティビティ一覧から自分の投稿に飛ぶとき、いま開いているチャンネルと同じならページ遷移せずその場でジャンプし、別チャンネルなら通常どおり画面遷移するための判定材料（今いるチャンネル名）を渡せるようにします。
- **影響範囲**: grep の結果、`currentChannelSlug` は sidebar.tsx と activity-modal.tsx の2ファイル間でしか使われていません。ActivityModal の利用箇所も sidebar.tsx 1箇所のみ。ルート構造は `/[workspace]/[channel]` の2階層で、sidebar.tsx 内の他箇所（252行目）でも全く同じ `pathname.split("/")[2]` で channel slug を取り出しており、既存の慣習どおりです。仮に `/workspace/settings` のような非チャンネル画面が将来増えても、ActivityModal は「同一なら遷移しない」判定にしか使っていないため、誤検知しても挙動上は別チャンネル扱いでルーター遷移に進むだけで、データ消失や誤操作にはつながりません。
- **LEVEL**: 軽微
- **根拠**: アクティビティモーダルの遷移判定にしか使われず、影響範囲は sidebar.tsx と activity-modal.tsx の2ファイル内に閉じている。


## ステータス

ダイアログタイムアウト → スキップ
