# src/app/(workspace)/[workspace]/[channel]/components/channel-view.tsx (2026-05-12T15:35:23)

**種別**: Git diff (vs HEAD)


## 変更前 diff

```
diff --git a/src/app/(workspace)/[workspace]/[channel]/components/channel-view.tsx b/src/app/(workspace)/[workspace]/[channel]/components/channel-view.tsx
index 6dbb000..b9b57c3 100644
--- a/src/app/(workspace)/[workspace]/[channel]/components/channel-view.tsx
+++ b/src/app/(workspace)/[workspace]/[channel]/components/channel-view.tsx
@@ -952,8 +952,8 @@ export function ChannelView({ channel, initialMessages, currentUserId, initialLa
       setMessages((prev) => mergeById(prev, fresh));
     }
 
-    // マウント直後に1回必ず走らせる
-    syncMissedMessages();
+    // マウント直後の同期は少し遅延させる（初期描画と遷移の負荷を分離）
+    const mountTimer = setTimeout(syncMissedMessages, 1500);
 
     function onVisible() {
       if (typeof document === "undefined") return;
@@ -977,6 +977,7 @@ export function ChannelView({ channel, initialMessages, currentUserId, initialLa
     return () => {
       cancelled = true;
       clearInterval(poll);
+      clearTimeout(mountTimer);
       document.removeEventListener("visibilitychange", onVisible);
       window.removeEventListener("focus", onVisible);
       window.removeEventListener("huddle:appResumed", onAppResume);

```


## Codex レビュー

- [優先度: 後で] `onVisible` / `onAppResume` / `poll` が 1.5 秒以内に `syncMissedMessages()` を呼ぶと、`mountTimer` でも再度同期が走り、同じ API を短時間に重複実行します。`syncMissedMessages()` 実行前に `clearTimeout(mountTimer)` する、または `hasSyncedOnce` / `inFlight` フラグで初回同期を一度にまとめるとよいです。

- [優先度: 任意] `1500` がマジックナンバーなので、`const INITIAL_SYNC_DELAY_MS = 1500` のように名前を付けると、遅延の意図と調整箇所が明確になります。


## 影響分析 (Claude read-only)

- **何が変わるか**: チャンネルを開いた直後に裏で「見逃したメッセージの取り直し」処理が即座に走っていたのを、1.5秒だけ遅らせます。チャンネル切り替えの最初の表示が軽くなり、画面遷移がよりスムーズに感じられるようになります。
- **影響範囲**: 変更箇所は ChannelView コンポーネント内のマウント時タイマー処理のみで、syncMissedMessages はこのファイル内のローカル関数です（grep の結果、外部ファイルでの参照はコメント内の言及のみで、実コードからの呼び出しはありません）。他のページ（DM、ワークスペース設定など）や他コンポーネントへの影響はありません。
- **LEVEL**: 軽微
- **根拠**: syncMissedMessages は ChannelView 内のクロージャで、外部 export や他ファイルからの呼び出しはない。


## ステータス

ユーザーが『スキップ』選択
