# src/app/(workspace)/[workspace]/board/components/board-canvas.tsx (2026-05-15T20:40:20)

**種別**: ファイル全体


## 変更前 diff

```
"use client";

import { useMemo } from "react";
import type { BoardNoteWithProfile } from "@/lib/supabase/types";
import { StickyNote } from "./sticky-note";

type Props = {
  notes: BoardNoteWithProfile[];
};

export function BoardCanvas({ notes }: Props) {
  // カテゴリ別にグループ化（未分類は末尾）
  const grouped = useMemo(() => {
    const map = new Map<string, BoardNoteWithProfile[]>();
    const uncategorized: BoardNoteWithProfile[] = [];

    for (const note of notes) {
      if (!note.category) {
        uncategorized.push(note);
      } else {
        const list = map.get(note.category);
        if (list) {
          list.push(note);
        } else {
          map.set(note.category, [note]);
        }
      }
    }

    const sorted = [...map.entries()].sort((a, b) => b[1].length - a[1].length);
    if (uncategorized.length > 0) {
      sorted.push(["未分類", uncategorized]);
    }
    return sorted;
  }, [notes]);

  if (notes.length === 0) {
    return (
      <div className="flex-1 flex items-center justify-center text-muted px-6">
        <div className="text-center">
          <div className="text-4xl mb-3">📝</div>
          <p className="text-sm">まだ付箋がありません</p>
          <p className="text-xs mt-1 text-muted/70">チャットでアイディアを投稿してください</p>
        </div>
      </div>
    );
  }

  return (
    <div className="flex-1 overflow-y-auto p-4">
      {grouped.map(([category, categoryNotes]) => (
        <div key={category} className="mb-6">
          {/* カテゴリヘッダ */}
          <div className="flex items-center gap-2 mb-3">
            <h3 className="text-sm font-bold text-foreground/80">
              {category}
            </h3>
            <span className="text-xs bg-foreground/10 text-foreground/50 rounded-full px-2 py-0.5">
              {categoryNotes.length}
            </span>
          </div>
          {/* 付箋グリッド */}
          <div className="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 xl:grid-cols-5 gap-3">
            {categoryNotes.map((note) => (
              <StickyNote key={note.id} note={note} />
            ))}
          </div>
        </div>
      ))}
    </div>
  );
}

```


## Codex レビュー

- [優先度: 今すぐ] `grouped.push(["未分類", uncategorized])` は、実データの `note.category === "未分類"` と衝突します。React の `key={category}` が重複し、同名カテゴリが2セクションに分裂します。内部キーと表示名を分けるか、未分類用に `__uncategorized__` のような sentinel を使ってください。

- [優先度: 後で] カテゴリの並び順が件数だけなので、同数カテゴリの順序が `notes` の入力順に依存します。表示の揺れを避けるなら `b[1].length - a[1].length || a[0].localeCompare(b[0], "ja")` のようにタイブレークを入れると安定します。

- [優先度: 後で] 上記のエッジケースをテストに追加してください。具体的には「カテゴリ `"未分類"` の付箋」と「`category` なしの付箋」が同時にある場合に、キー衝突せず期待した表示になることを確認するテストです。


## 影響分析 (Claude read-only)

- **何が変わるか**: 付箋ボードで「未分類」という名前のカテゴリを誰かが実際に作った場合に、本来別枠の「分類なし付箋」と表示がごちゃ混ぜになり一部が消えたように見える不具合を防ぎます。あわせて、付箋数が同じカテゴリ同士の並び順が毎回ブレないように安定させます。
- **影響範囲**: grep で BoardCanvas / board-canvas を検索した結果、この部品を使っているのは board-layout.tsx の1ファイルだけで、しかも渡しているデータ（付箋一覧）の形は今回変わりません。修正は付箋の仕分け・並べ替えという board-canvas.tsx 内部の処理だけなので、チャンネルやDMなど他の画面には影響しません。
- **LEVEL**: 軽微
- **根拠**: BoardCanvas は board-layout.tsx からのみ参照され、変更は同コンポーネント内部のグループ化処理に閉じており受け渡しの形は不変のため。


## ステータス

ユーザーが『スキップ』選択
