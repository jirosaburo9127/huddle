# src/components/sidebar.tsx (2026-05-13T22:23:15)

**種別**: Git diff (vs HEAD)


## 変更前 diff

```
diff --git a/src/components/sidebar.tsx b/src/components/sidebar.tsx
index fad6e04..4b84db6 100644
--- a/src/components/sidebar.tsx
+++ b/src/components/sidebar.tsx
@@ -833,7 +833,11 @@ export function Sidebar({
             {hitorigotoChannel && (
               <Link
                 href={`/${workspaceSlug}/${hitorigotoChannel.slug}`}
-                onClick={() => setSidebarOpen(false)}
+                onClick={() => {
+                  if (pathname === `/${workspaceSlug}/${hitorigotoChannel.slug}`) {
+                    setSidebarOpen(false);
+                  }
+                }}
                 className="lg:hidden relative shrink-0 mr-2"
               >
                 {/* 吹き出し本体 — pill 型 + 控えめな影 */}
@@ -1030,7 +1034,9 @@ export function Sidebar({
             workspaceSlug={workspaceSlug}
             pathname={pathname}
             unreadState={unreadState}
-            onNavigate={() => setSidebarOpen(false)}
+            onNavigate={(isActive) => {
+              if (isActive) setSidebarOpen(false);
+            }}
             channelMembersMap={channelMembersMap}
             workspaceMembers={members}
             onOpenMembers={(id) => setMembersModalChannelId(id)}
@@ -1122,7 +1128,9 @@ export function Sidebar({
               <Link
                 key={dm.id}
                 href={href}
-                onClick={() => setSidebarOpen(false)}
+                onClick={() => {
+                  if (isActive) setSidebarOpen(false);
+                }}
                 className={`
                   flex items-center gap-2 px-3 py-2 text-base rounded-xl mx-2 transition-colors
                   ${
@@ -1760,7 +1768,7 @@ type ChannelCategoryListProps = {
   workspaceSlug: string;
   pathname: string;
   unreadState: Record<string, number>;
-  onNavigate: () => void;
+  onNavigate: (isActive: boolean) => void;
   // channel.id → 所属ユーザーID配列
   channelMembersMap: Record<string, string[]>;
   // ユーザーID → プロフィール解決用
@@ -1926,7 +1934,7 @@ function ChannelCategoryList({
                   >
                     <Link
                       href={href}
-                      onClick={onNavigate}
+                      onClick={() => onNavigate(isActive)}
                       className={`flex items-center min-w-0 flex-1 px-3 py-2 text-base ${
                         isActive
                           ? "text-accent"

```


## Codex レビュー

- [優先度: 今すぐ] `src/components/sidebar.tsx:833`, `1034`, `1128`, `1934` の条件が逆方向です。現在は「すでに開いているページをクリックした時だけ」サイドバーを閉じ、別ページへ遷移するクリックでは閉じません。モバイルで遷移後もサイドバーが残るため、従来通り `setSidebarOpen(false)` を常に呼ぶか、少なくとも `!isActive` でも閉じるようにしてください。

- [優先度: 今すぐ] `ChannelCategoryListProps.onNavigate` の `isActive` 引数は呼び出し側で「閉じるべきか」の判定に使われていますが、名前から意図が読み取りにくく、今回のような誤実装を誘発します。`onNavigate={() => setSidebarOpen(false)}` に戻すか、必要なら `onItemClick({ href, isActive })` のように用途が分かる名前にしてください。

- [優先度: 後で] モバイルサイドバーのクリック挙動テストが不足しています。非アクティブなチャンネル/DM/ひとりごとリンクをクリックした時に `setSidebarOpen(false)` 相当で閉じることを、少なくとも対象3系統で回帰テストしてください。


## 影響分析 (Claude read-only)

- **何が変わるか**: モバイルでチャンネル/DM/ひとりごとのリンクを押した時に、サイドバーを閉じるかどうかの条件を変える変更です。ただしレビュアー指摘の通り条件が逆になっており、現状の差分のままだと「すでに開いているページを押した時だけ閉じる」「別の場所へ移動する時は閉じない」という意図と反対の挙動になります（要修正）。
- **影響範囲**: `ChannelCategoryList` と `onNavigate` は grep の結果 `src/components/sidebar.tsx` の内部だけで使われており、他ファイルからの参照はありません（ヒットしたのは `.huddle-reviews/` 配下のレビュー記録のみ）。`Sidebar` 本体の Props は変更していないので、`Sidebar` を呼んでいる他ページへの影響もありません。影響範囲はモバイル時のサイドバー開閉挙動のみで、PC（lg以上）の見た目・操作には影響しません。
- **LEVEL**: 軽微
- **根拠**: 変更は `sidebar.tsx` 内クロージャに閉じており、外部 export や共通 util への変更は無いため。


## ステータス

ユーザーが『スキップ』選択
