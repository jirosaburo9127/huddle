# src/app/(workspace)/[workspace]/[channel]/loading.tsx (2026-05-12T19:07:53)

**種別**: ファイル全体


## 変更前 diff

```
export default function ChannelLoading() {
  return (
    <div className="flex h-full flex-col bg-background">
      <header className="flex items-center px-3 sm:px-4 py-3 lg:py-0 lg:h-14 border-b border-border bg-header shrink-0">
        <div className="h-5 w-28 rounded-md bg-border/20" />
      </header>

      <div className="relative flex-1 overflow-hidden">
        <div className="absolute inset-x-0 top-0 h-px overflow-hidden bg-border/30">
          <div className="h-full w-1/3 animate-[channel-loading_0.9s_ease-in-out_infinite] rounded-full bg-accent/60" />
        </div>
      </div>

      <div className="shrink-0 px-3 sm:px-4 pb-3 sm:pb-4">
        <div className="h-12 rounded-xl border border-border/70 bg-input-bg/70" />
      </div>
    </div>
  );
}

```


## Codex レビュー

- [優先度: 後で] ルートの `<div>` に `aria-busy="true"` を付けると、支援技術にロード中状態を伝えやすくなります。例: `<div className="..." aria-busy="true">`。

- [優先度: 後で] `animate-[channel-loading_0.9s_ease-in-out_infinite]` は `@keyframes channel-loading` が未定義だと無効になります。定義がこの変更に含まれないなら、既存の `animate-pulse` などに寄せるか、グローバルCSS/Tailwind設定に keyframes を追加してください。

- [優先度: 任意] ユーザーの `prefers-reduced-motion` に配慮するなら、アニメーション要素に `motion-reduce:animate-none` を追加してください。


## 影響分析 (Claude read-only)

- **何が変わるか**: チャンネルを開くときに、画面全体が真っ白に見える瞬間を減らし「読み込み中」とわかる見た目を表示します。具体的には、上に細いバーがすーっと動くアニメーションと、入力欄の枠だけを先に出して、コンテンツが届くまでのチラつきや空白感を抑えます。
- **影響範囲**: このファイルは Next.js の規約上「チャンネルページ読み込み中に自動で出る画面」として使われる専用ファイルで、他ファイルから直接 import されていません（ChannelLoading で検索しても本ファイル1件のみ）。アニメーションで使う `channel-loading` キーフレームは `src/app/globals.css:115` に既に定義済みなので、Codex の懸念（keyframes 未定義）は実害なしです。他のページ（DM・ワークスペース設定など）には影響しません。
- **LEVEL**: 影響なし
- **根拠**: 同ルート配下のローディング専用ファイルで外部参照なし、依存するkeyframesもglobals.cssに定義済み。


## ステータス

ユーザーが『スキップ』選択
