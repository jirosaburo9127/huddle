# src/components/sidebar-loader.tsx (2026-05-13T23:06:40)

**種別**: Git diff (vs HEAD)


## 変更前 diff

```
diff --git a/src/components/sidebar-loader.tsx b/src/components/sidebar-loader.tsx
index b1dedab..ad3f036 100644
--- a/src/components/sidebar-loader.tsx
+++ b/src/components/sidebar-loader.tsx
@@ -5,6 +5,7 @@ import { Sidebar } from "@/components/sidebar";
 import { KeyboardShortcuts } from "@/components/keyboard-shortcuts";
 import { BottomTabBar } from "@/components/bottom-tab-bar";
 import { MainPane } from "@/components/main-pane";
+import { MobileDetailTransition } from "@/components/mobile-detail-transition";
 
 type WorkspaceShellProps = {
   workspaceSlug: string;
@@ -123,6 +124,7 @@ export async function WorkspaceShell({ workspaceSlug, children }: WorkspaceShell
       <KeyboardShortcuts workspaceId={result.workspace.id} workspaceSlug={workspaceSlug}>
         <MainPane>{children}</MainPane>
       </KeyboardShortcuts>
+      <MobileDetailTransition />
       <BottomTabBar workspaceSlug={workspaceSlug} workspaceId={result.workspace.id} currentUserId={user.id} members={result.members || []} />
     </>
   );

```


## Codex レビュー

- [優先度: 後で] `MobileDetailTransition` の追加に対するテストが不足しています。`WorkspaceShell` がモバイル遷移用コンポーネントを常に描画するようになるため、少なくとも `sidebar-loader` のレンダリングテストか E2E で、ワークスペース画面に `MobileDetailTransition` が含まれ、`BottomTabBar` と共存して表示崩れしないことを確認してください。

- [優先度: 任意] `MobileDetailTransition` がモバイル専用の副作用コンポーネントなら、コンポーネント側で `md:hidden` 相当や media query 判定を持たせて、デスクトップで不要な DOM・イベントリスナー・アニメーション処理が走らないようにしてください。今回の配置だと全画面サイズで常時マウントされます。


## 影響分析 (Claude read-only)

- **何が変わるか**: モバイルで一覧画面からチャンネル詳細を開くときに、読み込み中の「中間ページ」（タイトルとスケルトン）を表示できる仕掛けを、ワークスペース画面全体に組み込みます。これでタップ直後の体感がより滑らかになります。
- **影響範囲**: 追加された MobileDetailTransition は新規コンポーネントで、grep の結果 sidebar-loader.tsx 以外から参照されていません。WorkspaceShell は layout.tsx からのみ呼ばれるワークスペース全体の土台で、今回の変更は要素を1つ足すだけ（既存の Sidebar / MainPane / BottomTabBar は無変更）です。コンポーネント本体に lg:hidden が入っており、また pendingDetailOpen が立っていない時は null を返すため、PC 側や非遷移時に余計な DOM は出ません。
- **LEVEL**: 軽微
- **根拠**: 新規コンポーネントを土台に1行追加するのみで、他箇所への参照変更や挙動変更がない。


## ステータス

ユーザーが『スキップ』選択
