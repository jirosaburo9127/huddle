# src/app/(workspace)/[workspace]/[channel]/loading.tsx (2026-05-13T22:54:45)

**種別**: Git diff (vs HEAD)


## 変更前 diff

```
diff --git a/src/app/(workspace)/[workspace]/[channel]/loading.tsx b/src/app/(workspace)/[workspace]/[channel]/loading.tsx
index 63bf09f..bc4bfde 100644
--- a/src/app/(workspace)/[workspace]/[channel]/loading.tsx
+++ b/src/app/(workspace)/[workspace]/[channel]/loading.tsx
@@ -1,16 +1,4 @@
-"use client";
-
-import { useEffect } from "react";
-import { useMobileNavStore } from "@/stores/mobile-nav-store";
-
 export default function ChannelLoading() {
-  const setSidebarOpen = useMobileNavStore((s) => s.setSidebarOpen);
-
-  // loading表示 = チャンネルへの遷移が始まった → サイドバーを閉じて詳細面をスライドイン
-  useEffect(() => {
-    setSidebarOpen(false);
-  }, [setSidebarOpen]);
-
   return (
     <div className="flex h-full flex-col bg-background">
       <header className="flex items-center px-3 sm:px-4 py-3 lg:py-0 lg:h-14 border-b border-border bg-header shrink-0">

```


## Codex レビュー

- [優先度: 今すぐ] `src/app/(workspace)/[workspace]/[channel]/loading.tsx`: モバイルでチャンネル遷移時に `setSidebarOpen(false)` が呼ばれなくなり、サイドバーが開いたままローディング/遷移先詳細面を覆う可能性があります。副作用を削除するなら、チャンネルリンクのクリックハンドラやルート変更を監視する既存のクライアントコンポーネント側で `setSidebarOpen(false)` を実行してください。

- [優先度: 後で] 上記挙動の回帰テストがあると安全です。モバイル幅でサイドバーを開く → チャンネルを選択 → 遷移中または遷移後にサイドバーが閉じている、というテストを追加してください。


## 影響分析 (Claude read-only)

- **何が変わるか**: チャンネル一覧→チャンネル遷移のローディング画面でサイドバーを自動で閉じる仕組みを削除します。実は他の場所（チャンネルリンクのクリック処理、遷移先の画面マウント時の処理）でも同じことをやっているため二重実装でした。これを整理する変更です。
- **影響範囲**: grep で `setSidebarOpen` の呼び出しを確認したところ、サイドバー側（`sidebar.tsx`）でチャンネル/DMリンクをクリックした時点で `setSidebarOpen(false)` が8箇所で呼ばれており（チャンネルリンク・DMリンク・お気に入り等）、さらに遷移先の `channel-view.tsx` がマウントされた時点（308行目）でも `setSidebarOpen(false)` が呼ばれます。`bottom-tab-bar.tsx` の他タブへの遷移もそれぞれ独自に閉じる処理を持っています。よってモバイルで通常のチャンネル選択経由ならサイドバーは閉じます。ただしレビュアー指摘通り、ブラウザの戻る/進むや外部リンク・プログラム的なルート変更で同じチャンネル以外に飛ぶ稀なケースでは、`channel-view.tsx` のマウント完了までの間（数百ミリ秒）ローディング画面の上にサイドバーが残って見える可能性があります。
- **LEVEL**: 軽微
- **根拠**: 通常の遷移経路は別箇所で `setSidebarOpen(false)` が走るため実害はほぼなく、外部から参照される export もなし（default export のページコンポーネントのみ）。


## ステータス

ユーザーが『スキップ』選択
